# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Changed

- **Dev channel (branch `poc/kagent-main`): Substrate comes from the Giant Swarm line.** `kagent.substrateWorkerPool.workerImage` is `ghcr.io/giantswarm/substrate/ateom-gvisor:0.0.27-dev.giantswarm.2026-09-10.19-33-37.h734ec53` — the gVisor worker image the fork [giantswarm/substrate](https://github.com/giantswarm/substrate) publishes from its `giantswarm` branch (upstream kagent-dev/substrate 0.0.26, the version the kagent line's `go.mod` pins, plus the cherry-picked kagent-dev/substrate#33; its `FORK.md` is the ledger of pin, carried patches and published digests) instead of upstream's `ghcr.io/kagent-dev/substrate/ateom-gvisor:v0.0.26`. The fork tags images with the bare version. The Substrate control plane a cluster runs (installed by agentlab, later by meta chart 4.0) must be the same version: the README's Substrate prerequisite names the fork's charts. The render is otherwise unchanged. The ATS smoke clears `components.agent-platform-connectivity.semverFilter` and `gitops.self.semverFilter` where it pins the candidate's exact version: on this line the defaults admit only `poc-kagent-main` builds, so the candidate of a pull request against the line (a `-dev.<pr-branch>.` version) matched nothing and every smoke on such a pull request failed at the install — pushes to the branch itself were unaffected.
- **Dev channel (branch `poc/kagent-main`, the kagent API v2 line).** The chart's defaults follow the RFC dev builds of its sibling POC branches: `components.agent-platform-connectivity` `versionRange: ">=3.0.0-0 <4.0.0-0"`, `components.backstage`, `components.model-manager`, `components.agent-manager` `versionRange: ">=0.0.0-0 <1.0.0-0"`, each with `semverFilter: ".*-dev\\.poc-kagent-main\\..*"`, and `gitops.self.semverFilter` the same, so a consumer selects the meta chart's dev channel once and every sibling follows its own branch's newest build. kagent is kagent `main` from the kagent fork's `poc/agent-platform` branch, published under `ghcr.io/giantswarm` (`components.kagent` and the new roster entry `components.kagent-crds`: `oci://ghcr.io/giantswarm/kagent/helm`, `versionRange: ">=0.11.0-0 <0.12.0-0"`, `semverFilter: ".*-dev\\.poc-agent-platform\\..*"`; kagent `dependsOn` kagent-crds, the connectivity release too; the render refuses kagent on without kagent-crds); the `kagent:` block carries the fork's image names under `registry: ghcr.io` with an explicit `tag`, `controller.substrate.*` (Agent Substrate in `ate-system`, a cluster prerequisite), the `substrateWorkerPool` (`kagent-default`, 4 gVisor workers), pgvector on the bundled Postgres, `serviceMonitor.enabled: false` (kagent `main` serves no metrics listener) and the two platform `harnesses[]` (Go ADK and Claude Code, digest-pinned); `kagent-crds:` keeps its kmcp and Substrate CRD subcharts off. The filter constant is defined once, in the `components:` preamble of `values.yaml`, together with the switch to `.*-bd384adaft.*` once gitsemver ships the RFC's final tag schema. **The release of this line resets all of it**: every `semverFilter` goes and the ranges return to the stable lines (`agent-platform-connectivity` `">=1.0.0"`, the three siblings `"0.x"`, kagent to the GS wrapper on gsoci). The customer BOM example pins the dev builds and clears the filters (an exact pin with a filter matches nothing).
- The connectivity chart wires kagent `main` (`kagent.dev/v1alpha3`) — a POC branch, not for a `main` merge while the fleet runs the kagent 0.2.x wrapper (v1alpha2). The shared muster `RemoteMCPServer`, `kagent.remoteMcpServers[]` and `kagent.modelConfigs[]` render as `v1alpha3`; the muster server moves from the release namespace into the kagent namespace and loses `allowedNamespaces` — on `main` an `AgentTemplate`'s `tools[].mcp.server` binds a server of its own namespace only, and the agents are `AgentTemplate`s agent-manager and the portal write there, as the person. New `kagent.harnesses[]` renders the platform's `Harness` objects: runtime type (`kagent | claude | codex | byo`), a digest-pinned `workload.image` (a tag fails the render, as the CRD would fail it at admission), optional `command`/`args`/`env`, the Substrate `workerPoolRef` and `snapshotPolicy.location`, and the `allowedAgentTemplates` selector (default `kagent.dev/harness: <name>`); the meta chart holds the key back from the kagent release (`omitKeys`). Both charts declare a passthrough `kagent-crds` values block for the kagent-crds component an installation adds to the roster (kagent `main` ships its CRDs as their own chart, which the kagent release `dependsOn`); without it the meta chart's root schema refused those values. The two kagent declarative-agent Kyverno `ClusterPolicy`s and the seccomp `PolicyException` are gone with the `Agent` CRD, the per-agent Deployments and the config Secrets they mutated — agents run as Substrate actors in gVisor worker pods — and with them `kyvernoPolicies.seccompPolicyName` / `seccompRuleNames`. The Backstage app-config's `agentPlatform.kagent.installations.<installation>.apiBaseUrl` is `https://<agentgateway hostname><pathPrefix>` with no `/api` suffix: the controller serves gRPC, gRPC-Web and `/mcp` on one port and no REST. The kagent network policies are unchanged and their agent-pod selectors (`app: kagent`) are documented as v1alpha2-shaped (actor traffic leaves through Substrate's `atenet-egress`; unverified in the POC). `make verify-kagent-harnesses` asserts the Harness render and its guards; `verify-kagent-discovery` the API version, the kagent namespace and the absence of `allowedNamespaces`; `verify-modes` the remaining kyverno.io object count.

### Added

- `components.<name>.semverFilter` and `gitops.self.semverFilter` select a dev channel: a Go regexp rendered as `OCIRepository.spec.ref.semverFilter`, which Flux applies to the registry's tags before it evaluates the semver range. With a prerelease-admitting range (`>=3.0.0-0 <4.0.0-0`) and the filter of one branch's dev builds (`.*-dev\.<sanitized-branch>\..*`) a component — or, under self-management, the chart itself — follows that branch's newest build and never falls back to a stable tag. Unset by default. The dead `components.<name>.filterTags` block (an ImagePolicy field, never an OCIRepository one, undeclared in the schema) is gone.
- `gitops.forbidPinnedLoginConnector` (default `false`): with `true` the render fails when `muster.muster.oauth.server.dex.connectorId` is set, naming the key. A pinned connector makes muster (through mcp-oauth) append `connector_id` to every Dex authorization request, so Dex's connector chooser never appears; on an installation whose Dex serves several identity providers — the operator's and the tenant's — the pin hides every other connector, and people from those providers either cannot sign in to muster or, when the pinned connector is not the provider their MCP-server allowlists (`allowedGroups`, RBAC) are written for, receive a token without those groups. Backstage and the kagent UI leave the choice to Dex; the knob lets a fleet template hold muster to the same rule against a per-installation override, at render time and with the same shape as `gitops.forbidInlineSecrets`. Meta-package plumbing: not forwarded to the muster release. **No installation needs to act**: off by default, every render is unchanged. `make verify-login-connector` asserts the four cases (default renders and forwards a pin; on plus a pin fails naming the key and never repeating the value; on plus an empty or absent key renders; the knob reaches no child release).

### Fixed

- **Dev line (`poc/kagent-main`): the meta chart forwards neither `agentChart` nor `flux` to agent-manager.** It forwarded the release line's `agent-manager.agentChart` (`ociUrl`, `semver`) from `values.yaml` and a derived `agent-manager.flux.helmReleaseServiceAccount` (from `kagent.fluxServiceAccountName`, `agent-platform.componentDerivedValues`) to the agent-manager release, but the agent-manager `poc/kagent-main` chart — kagent API v2: agents are `kagent.dev/v1alpha3` `AgentTemplate` CRs plus their carrier `RemoteMCPServer`, written directly as the caller; no agent chart, no agent `HelmRelease` — validates its values with a closed schema that declares neither, so helm-controller refused every install of the release: `values don't meet the specifications of the schema(s): agent-manager: - at '': additional properties 'agentChart', 'flux' not allowed` (measured on the first `agentlab --chart-branch poc/kagent-main` bring-up from published artifacts; every other HelmRelease Ready). Nothing had rendered the block against the real chart: the POC lab ran the released agent-manager chart with a swapped dev image, and the ATS smoke keeps `components.agent-manager.enabled: false` on this line. `values.yaml` no longer sets `agent-manager.agentChart`; the helper derives nothing on this line and instead fails the render when a values file sets `agent-manager.flux.*` or `agent-manager.agentChart.*`, naming the key; the tenant identity (`kagent.fluxServiceAccountName`: ServiceAccount, RoleBinding, the portal's `agentPlatform.fluxServiceAccountName`) stays rendered. The coverage hole is closed: `make verify-components-charts` (`tests/verify-components-charts.py`, in CI) also pulls the **agent-manager and model-manager** charts, at the dev channel's resolution and at the BOM pin, and renders each with the exact values the meta chart forwards to it — a schema violation fails the target (against the unfixed head it reproduced the message above) — and `make verify-managers` / `make verify-identity` assert the two keys are absent from the forwarded values and refused when set. The release of this line restores both keys with the agent chart.
- The data-plane `NetworkPolicy` admits the Prometheus scrape of the agentgateway data plane (pod port 15020, `/metrics`), in both flavours. The `PodMonitor` this chart renders targets that port, but the policy's only ingress rule listed the Gateway's listener ports — 8080, and 8081 once `llmRouting` is on — so Cilium dropped every scrape: measured on graveler, the target reported `up=0` with `scrape_samples_scraped=0` for as long as the monitor had existed, `JobScrapingFailure` sat pending against it, and not one GenAI or HTTP series ever reached the tenant's Mimir. The monitor was therefore inert from the moment it was added, and the LLM cutover's whole point — tokens, cost and latency per model call — produced nothing on an installation that had done both rollout steps correctly. A second `fromEntities: ["cluster"]` rule now admits 15020, exactly as the sibling controller policy admits the controller's own 9092/9093; the port is fixed by the agentgateway controller (it names the container port `metrics` and sets the `prometheus.io/*` annotations), not an operator choice, so it is written literally rather than as a value. Unconditional, like that sibling rule, because a cluster may scrape by pod annotation with no `PodMonitor` at all. **An installation with the agentgateway component on gains one ingress rule on that policy and starts reporting the data plane's metrics; nothing else changes and no pod rolls.** `make verify-llm-routing` asserts the port in both flavours and with `llmRouting` off, where the monitor already renders; reverting either template fails it.
- `gateway.parameters.dataPlaneResources` is settable through the meta chart (#303). The connectivity chart reads the key (the agentgateway data-plane container's `ephemeral-storage` requests and limits, merged onto the container through `AgentgatewayParameters`) and its schema declares it, but the meta chart's `values.yaml` did not, so the meta chart's generated `values.schema.json` (`gateway.parameters` is `additionalProperties: false`) refused it at render time — `additional properties 'dataPlaneResources' not allowed` — although `forwardAllValues` would have carried it to the connectivity release unchanged; an installation migrating from the standalone chart, whose umbrella accepted the key, hit it. The meta chart now declares the key with the connectivity chart's defaults (`requests.ephemeral-storage: 50Mi`, `limits.ephemeral-storage: 512Mi`), so the rendered platform objects are unchanged until an installation overrides them. `make verify-meta`'s schema-symmetry check compared the two charts' top-level keys only and missed the nested key; it is now `tests/verify-schema-symmetry.py`, which walks the connectivity schema's `properties` (and array `items`) under every block the meta chart forwards and fails on any nested key the meta schema rejects — and the other way round, on any forwarded meta key the connectivity schema does not declare (the roster travels as `components.<name>.enabled` only) — with negative cases proving both directions fail, naming the path. The walk found no other gap.
- The kagent controller metrics `Service` the connectivity chart renders (`<release>-kagent-controller-metrics`, the target of its `ServiceMonitor`) selected the controller pods with the connectivity release's own `app.kubernetes.io/instance` label. Under the retired standalone umbrella every subchart shared one release name, so that matched; under the meta chart each component is its own release — the kagent pods carry `app.kubernetes.io/instance: kagent`, the component key the meta chart fixes as the release name — and the Service had no endpoints: the controller was never scraped on any installation with kagent and the monitors on (#305). The selector now names kagent's release through the new connectivity helper `agent-platform.kagent.releaseName`; the fallbacks of the controller route's backend host, the UI route's Service and the oauth2-proxy Service (`kagent.fullnameOverride` / `kagent.oauth2-proxy.fullnameOverride`, pinned in values.yaml, else `.Release.Name`) go through `agent-platform.kagent.fullname` / the release-name helper too — the same wrong assumption, latent behind the pins. No compatibility knob: the meta chart is the chart's only consumer. **No installation needs to act**: the Service's selector changes (a Service update, no pod rolls) and kagent controller scraping starts working; every other object is byte-identical. `make verify-global` asserts the selector and that no kagent template falls back to `.Release.Name`.
- A first `helm install` with `components.kagent.enabled` on a cluster without the `kagent` namespace deadlocked: the kagent chart installs into `kagent.namespaceOverride` while its `HelmRelease` targets the platform namespace (so `install.createNamespace` never creates `kagent`), and the one chart object that does create it — the connectivity chart's `Namespace` — sits in a release that `dependsOn` kagent. Every kagent attempt failed with `namespaces "kagent" not found` until the retries were exhausted, everything behind kagent waited, `helm install --wait` timed out (#306). With the bundled engine the meta chart now runs one `pre-install,pre-upgrade` hook Job, `<release>-kagent-namespace` (weight -8, `alpine/k8s`, as the hook ServiceAccount, whose own hook events gain `pre-install,pre-upgrade` while this hook renders): it creates the namespace when it is missing, waits out one that is still terminating (the reinstall right after an uninstall), and leaves an existing one alone. The `Namespace` is deliberately not an object of the meta release — the connectivity release renders and tracks it, adopting the hook-created one exactly as it adopts the one Giant Swarm's bases pre-create, and two Helm releases must never track one object. Not rendered with the engine off (the fleet's pure render is byte-identical; a cluster's own Flux gets the namespace out of band), with kagent off, with `kagent.namespaceOverride` empty or equal to the `HelmRelease`s' target namespace. Measured on kind (kagent on, no namespace): `helm install --wait` deployed, `helm upgrade` idempotent, `helm uninstall --wait` clean with the namespace gone through the connectivity release, reinstall clean. `make verify-engine` asserts the hook and its absences; the ATS kind smoke can turn kagent on from this version.
- The connectivity release's `Namespace` for kagent (`kagent.namespaceOverride`) carries `helm.sh/resource-policy: keep`. Helm deleted it with the release — on `helm uninstall` of the platform, taking the agents' workloads with it although the platform's teardown is documented to leave them behind orphaned, and, worse, as the **install remediation of a failed first install** of the connectivity release: helm-controller uninstalls a failed first install before it retries, and that uninstall removed the kagent namespace and every kagent object in it while the kagent release believed itself installed (measured on the ATS kind smoke, where a `PolicyException` without its `policy-exceptions` namespace failed the connectivity install). Kept, the namespace survives both; the release adopts it on a reinstall as it adopts a namespace the cluster created. **Management clusters see one annotation added to the `kagent` Namespace where kagent is on, nothing else.**
- The `helm.sh/chart` label of every object stays a valid label value at the versions the charts are actually installed under. helm-controller installs a chart with the OCI digest appended as build metadata (`3.20.0+8c89e1be4cbf`; the label helper turns `+` into `_`), and a branch build carries a long prerelease version (`3.19.1-dev.<branch>.<date>.<time>.h<sha>`, stamped by abs); the 63-character cut of `<name>-<version>` could land on that `_`, which the apiserver rejects ("must start and end with an alphanumeric character") for every object of the release — measured on the self-management adoption of a branch build, whose failed revision then also poisoned `helm uninstall` (the hooks of the last revision carried the label). The `chart` helpers of both charts and of the `flux-engine` subchart trim `_` after the cut as they trim `-` and `.`; `make verify-labels` (`tests/verify-labels.py`) packages both charts at versions whose cut lands on each separator and validates every label of the render. Published versions are short and were never affected; nothing changes in their render.
- The shared `RemoteMCPServer agent-platform/muster` reported `Accepted=False (ReconcileFailed … Unauthorized)` on every installation: the kagent controller lists a server's tools with its own request, which carries no bearer, and muster (an OAuth resource server) answers 401 (#299). While muster runs with OAuth on (`muster.muster.oauth.server.enabled`, the default) the connectivity chart now labels the CR `kagent.dev/discovery: disabled`, the opt-out from controller-side tool discovery contributed upstream (kagent-dev/kagent#2752, `0.10.x` backport kagent-dev/kagent#2753): a controller that knows the label reports `Accepted=True` (`DiscoveryDisabled`, empty inventory); an older one ignores the label and behaves as before. Agents are unaffected — they resolve tools at run time as the caller. A controller credential through `spec.headersFrom` was rejected on purpose: the kagent runtime applies static headers after the propagated caller token, so every agent would have acted as the controller's ServiceAccount (docs/authentication.md, "Tool discovery by the kagent controller"). `make verify-kagent-discovery` asserts the label follows the OAuth toggle and stays off the operator-defined `kagent.remoteMcpServers`.
- A fresh install with `components.kagent.enabled` and `postgres.enabled` deadlocked: the kagent controller mounts the CNPG Secret `kagent-pg-app` that the connectivity release renders, while connectivity `dependsOn` kagent for its CRDs, so the kagent install waited for a controller that could never start (`FailedMount`), timed out, was uninstalled and retried, and connectivity never installed. The kagent child HelmRelease now carries `install.disableWait: true` (new per-component key `components.<name>.installDisableWait`): the first install is reported once the manifests (CRDs, Deployment) are applied, connectivity proceeds, the controller starts as soon as the Cluster exists; upgrades keep waiting for the rollout. `make verify-meta` asserts it. Existing installations see only the changed HelmRelease spec, no Helm action.

### Removed

- The Argo CD render engine of the meta-package. `gitops.engine` accepts `flux` only (`enum: [flux]` in the schema, a template guard behind it); `templates/components.yaml` renders a Flux `OCIRepository` + `HelmRelease` per component and nothing else; `gitops.argo.project` / `gitops.argo.server` are gone from the values and the schema. The Argo `Application` branch was never verified against a running Argo CD and no installation selected it, and the platform cannot run on Argo CD in any case: agents are Flux objects on every path (the Backstage agent create flow and agent-manager write an `OCIRepository` + `HelmRelease` per agent). `gitops.engine: argo` now fails the render — the schema first, the guard with `gitops.engine=argo is not supported; flux is the only engine` — and `gitops.argo.*` fails the schema. `make verify-meta` asserts the refusal on both layers, that the Flux render carries no `argoproj.io` object and that an explicit `gitops.engine: flux` renders exactly the default; the Argo render assertions of `verify-meta` and `verify-presets` are gone with the branch. **No installation needs to act**: the Flux render is byte-identical. See UPGRADE.md.

### Fixed

- The kagent `Namespace` (`kagent.namespaceOverride`, when it differs from the release namespace) renders only while the kagent component is on — the last of the standalone generator's nine template patches, landed upstream. An installation without kagent no longer gets an empty, Helm-owned `kagent` namespace, and turning kagent off removes it with the release. **On a management cluster where kagent is off, the connectivity upgrade deletes the empty `kagent` namespace**; the fleet's hand-written `kagent-flux` identity that used to live there was handed over to the chart first (where kagent runs) or orphaned (where it does not), so nothing else is in it. Where kagent is on, nothing changes.
- Six of the text patches the standalone umbrella's generator applied to its copy of the connectivity templates were bugs here too and are fixed at their upstream location, so no generator is needed:
  - The kagent UI `HTTPRoute` (`kagent.uiRoute` without oauth2-proxy) targets the Service the kagent chart names from `kagent.fullnameOverride` (`kagent-ui`), not `<release>-ui` — as the oauth2-proxy branch and the controller route already did. The fleet runs the oauth2-proxy branch, so its render is unchanged.
  - The muster `/` `HTTPRoute` renders only while the muster component is on (`components.muster.enabled`): hostname-less on a shared Gateway, it blackholed every request when an umbrella turned muster off. The meta chart force-enables muster, so the fleet render is unchanged.
  - `ingress.mode: muster-direct` refuses the knobs that render agentgateway.dev objects or attach to the agentgateway Gateway — `kagent.controllerRoute.enabled`, `klausGateway.agentgatewayRoute.enabled`, and agent-platform-mcps' `agentgateway.enabled` with `mcpServers` set — naming the knob, instead of shipping objects the API server rejects on a cluster without the agentgateway CRDs (the model-manager / agent-manager routes already guarded themselves this way). `ci/test-klausgateway-obo-values.yaml` moves to `agentgateway-muster`, `ci/test-mcps-values.yaml` sets `agent-platform-mcps.agentgateway.enabled: false`; every management cluster runs `agentgateway-muster`.
  - The legacy component-toggle probe (`agent-platform.validateLegacyToggles`, both charts) reports a leftover `<block>.enabled` whatever its value, also `true` under a component that is on: neither chart has a Helm dependency, so no chart default is ever coalesced into those blocks and the key can only be the operator's. The "on + true passes" leg and its prose described an umbrella that feeds the blocks to real dependencies (the standalone patched both); `make verify-modes` asserts the strict probe in both charts.
  - The kserve API guard of the model-manager wiring defers to the platform's own KServe when it is bundled: with `components.kserve-resources` or `components.modelServing` on, that component's guards own the check (a missing API is then the first install) and the message names the components. The new `agent-platform.optionalComponentEnabled` helper treats a component absent from the roster as off — unlike `componentEnabled`, whose absent-is-on rule is for the force-on components.
  - The `agent-platform.serviceMonitor` helper's prose stated `default true`, which an umbrella flipped; with the knob defaulting to `auto` (#291) the prose says so and no umbrella has a reason to flip it.
- `kagent.oauth2ProxyIngress` is dropped from the values forwarded to the kagent chart (`components.kagent.omitKeys`). It is read by the connectivity chart only, and the flattened kagent 0.2.x chart rejects it (`additional properties 'oauth2ProxyIngress' not allowed`), so an installation that set it (glean) failed the kagent upgrade to 0.2.0. `tests/verify-kagent-wiring.py` now derives the umbrella-only keys from what the connectivity templates read under `.Values.kagent`, so a new one cannot be forwarded unnoticed.

### Added

- `llmRouting` (default `enabled: false`) in both charts: agent inference traffic through the installation's agentgateway, so the data plane emits GenAI metrics — tokens by type, cost in USD, request duration, time to first token — for every model call. **An installation that sets nothing renders byte-identically** (the golden check pins this). When on, the connectivity chart renders an `llm` listener on the data-plane Gateway (`llmRouting.listener`, default port 8081), an `AgentgatewayBackend` for the provider, an `HTTPRoute` pinned to that listener by `sectionName` and matching the provider path prefixes (`llmRouting.pathPrefixes`, default `/v1`) so it outranks the `agent-platform-mcps` catch-all route, which attaches to every listener of the same Gateway and wins an equal match, one Gateway-scoped `AgentgatewayPolicy` carrying the route-type map (`llmRouting.routes`) and the agent attribution labels (`llmRouting.metricLabels`), and the model-price ConfigMap (`llmRouting.modelCatalog`) that the Gateway's `AgentgatewayParameters` references. The gateway holds no provider credential: agent pods keep their own key and the client `x-api-key` header passes through, so reach to the listener grants no spend. Turning the listener on routes nothing by itself — the cutover is a second value, `kagent.providers.anthropic.config.baseUrl`, and rollback is its removal. The render fails when `llmRouting.enabled` is true without the agentgateway data plane, when the listener port collides with an existing one, or when `llmRouting.pathPrefixes` is empty or contains a bare `/`. See the connectivity chart README for the rollout.
- A `PodMonitor` for the agentgateway data plane, gated on the agentgateway component and the shared metrics helper — not on `llmRouting.enabled`, so the MCP path is scraped too and the monitor exists before an LLM cutover. The data plane serves Prometheus text on pod port 15020; the controller-provisioned Service exposes listener ports only, so a ServiceMonitor would match nothing. It carries the same tenant label as the kagent ServiceMonitor, which alloy-metrics discovers monitors by.
- `kagent.modelConfigs[].baseUrl` (optional): sends a chart-owned ModelConfig's traffic through the LLM listener. Without it an extra ModelConfig calls the provider directly and its usage is unmetered.
- **The ATS proves the product on every PR** (roadmap#4348, PRD Q8; `tests/ats/`, README "Development"). The kind smoke is the quick start on a bare cluster: the Gateway API CRDs, a lab Dex (`tests/ats/lab-dex.yaml`, ported from the standalone chart — static users, the platform identity Secret, a self-signed CA, a CoreDNS rewrite; Dex terminates TLS itself, there is no edge Gateway on kind) and an in-cluster registry the candidate archive is pushed to (`tests/ats/registry.yaml`, `helm push --plain-http`); `helm install --wait` with the bundled engine, muster and its OAuth server, dicebear, connectivity, kagent and agent-manager (`tests/test-values.yaml` + `tests/ats/values-kagent.yaml` + `tests/ats/values-round-trips.yaml`) and **self-management on against that registry** at the candidate's exact version (a branch build is a prerelease, which the derived range never matches — Masterminds semver). It asserts the install's promises (deployed, FluxInstance Ready, every component HelmRelease Ready as `agent-platform-flux`, the operator on the Flux CRDs, the kagent namespace), the **adoption** (`helm history` = the install + one adoption revision), the **auth round trip** (401 with the RFC 9728 chain; a Dex user through the OAuth password grant with the cross-client audience, and through muster's login flow — RFC 7591 registration, code + PKCE, the Dex form), the **agent round trips** (a declarative `Agent` Ready against the default ModelConfig with a placeholder key; agent-manager's `create_agent` through muster as the Dex user with the forwarded token → OCIRepository + HelmRelease of the agent chart in `kagent`, executed as `kagent-flux`, Ready → the `Agent` Ready — the Flux write path the standalone smoke never had), the **fixpoint** two intervals after the adoption (history unchanged, the values Secret and `helm get values` equal to the values used), the **refused `helm upgrade`** (the admission policy's message in Helm's output, no revision) and the **ordered teardown** (`helm uninstall --wait` within budget, no Flux CRD left, the operator CRDs remaining, no controller, no Job of the release, no release, the policy gone; the agents' HelmRelease objects gone with the CRDs, their Deployments and `Agent` objects orphaned in the kept kagent namespace). The **own-Flux scenario** (`tests/ats/test_own_flux.py`, the `functional` step) runs on the same cluster afterwards: Flux's source-controller + helm-controller from the pinned upstream manifest, the chart through a HelmRelease from the in-cluster registry with `components.flux.enabled: false` — Ready, the platform HelmReleases Ready under that Flux without a `serviceAccountName`, no operator, no FluxInstance, one helm-controller, the Flux CRDs' field managers exactly what `flux install` left — an agent through that Flux (OCIRepository + HelmRelease of the agent chart as `kagent-flux`, the `Agent` Ready), the render guard when the value is flipped on (the HelmRelease fails with `this cluster runs Flux; set components.flux.enabled=false or install the chart through it`; no operator, no FluxInstance, the platform and the agent still Ready, the Flux CRDs' content unchanged — helm-controller applies a chart's `crds/` before it renders, so it joins the CRDs' field managers with identical content, asserted and logged; flipped back it recovers) and the way back. `.ats/main.yaml` runs `smoke` and `functional`; `upgrade` stays skipped. The standalone chart's `tests/ats/test_smoke.py` has no assertion this suite lacks (`tests/ats/README.md`, the parity table).
- `components.<name>.insecure` (default absent): `OCIRepository.spec.insecure: true` for that component — a plain-HTTP registry, a lab's in-cluster one; never a public registry. The ATS uses it to install the connectivity chart of the checkout from the in-cluster registry (`components.agent-platform-connectivity.{repository,versionRange,insecure}`), so a PR's connectivity changes are tested on kind before they are released. `make verify-insecure`.

- **Self-management through the bundled Flux, on by default** (roadmap#4348, PRD Q5/Q5b): where the chart brings the engine, the engine also holds the chart. `gitops.self.enabled` (`auto` | `true` | `false`, default `auto` = follows `components.flux.enabled`; `true` with the engine off fails the render) renders the release's own `OCIRepository` + `HelmRelease` (`templates/self/release.yaml`, both named after the release, in the release namespace, run as `agent-platform-flux` — `gitops.serviceAccountName` overrides it — `chartRef` to the `OCIRepository`, `releaseName`/`targetNamespace` the release's, `install`/`upgrade.disableWait`, values from Secret `agent-platform-values` key `values.yaml` with `optional: false`). The `OCIRepository` follows `<gitops.self.repository>/agent-platform` (default `oci://gsoci.azurecr.io/charts/giantswarm`; `gitops.self.insecure` for a plain-HTTP lab registry) at `>=<installed version> <next major>.0.0`, derived from the running chart at render time (`gitops.self.versionRange` overrides; an offline `helm template` derives it from the placeholder `Chart.yaml` version), on `gitops.self.interval` (default `10m`). The bundled helm-controller adopts the CLI-installed release with one extra revision and rolls the chart forward inside its major without a `helm upgrade`. Measured on kind: install `--wait` 87 s, adoption in the second the release went `deployed`, a pushed version applied 47 s after the push with the new chart default in effect and the user's values intact, a rewritten values Secret applied within 20 s, uninstall `--wait` with the self `HelmRelease` present in under a minute, the upgrade from an engine-on 3.18.0 into this version adopted with the bracket held.
- **The install-time bracket**: the self `HelmRelease` is rendered `suspend: true` on `helm install` and whenever it does not exist yet (a guarded `lookup` — the upgrade of an engine-on installation into this version), never on a controller-driven upgrade; the post-install/post-upgrade hook `<release>-self-values` (weight 0) writes the **user-supplied** values of the revision in flight (`helm get values`, never the merged tree) into Secret `agent-platform-values`, labelled `reconcile.fluxcd.io/watch=Enabled`, by server-side apply, and — only while the `HelmRelease` is suspended — starts the detached Job `<release>-self-resume`, which polls `helm status` and resumes the `HelmRelease` once the release is `deployed` (or `failed`); still pending after 300 s it exits 1 *without* resuming. Neither is a chart object Helm's kstatus `--wait` would wait on. `helm template` renders `suspend: true`.
- **The Helm CLI is day-0 only — a `ValidatingAdmissionPolicy`** (`templates/self/policy.yaml`: `<release>-self-managed-<namespace>` + a binding of the same name scoped to the release namespace, `failurePolicy: Fail`) on the CREATE of Secrets of type `helm.sh/release.v1` labelled `name=<release>`: admitted for `system:serviceaccount:<namespace>:agent-platform-flux` (the identity the bundled helm-controller impersonates) or while the namespace carries `agent-platform.giantswarm.io/helm-cli=allow`. `helm upgrade` and `helm rollback` fail in about a second with the reason in Helm's own error output (the message names Secret `agent-platform-values` and the hand-back) and write no revision; UPDATE/DELETE pass, so does `helm uninstall`. Needs Kubernetes ≥ 1.30: the render refuses an older cluster while self-management is on. A render guard also refuses the annotation together with self-management on (two writers on one release). The guard-Job alternative (a pre-upgrade hook reading the pending storage Secret's `managedFields` manager) stays documented in UPGRADE.md as the fallback should the policy ever be ruled out.
- **The hand-back / escape hatch**: `kubectl annotate namespace <ns> agent-platform.giantswarm.io/helm-cli=allow`, then `helm upgrade … --set gitops.self.enabled=false --force-conflicts` (once; helm-controller co-owns the fields) hands the release back to the CLI without an uninstall — the new pre-upgrade hooks suspend the self `HelmRelease` before Helm deletes it, so helm-controller drops its finalizer; the value stays in the values file (README "Self-management").
- **Teardown extended** (`templates/hooks/self.yaml`, rendered whenever the engine is on): weight -6 `<release>-self-stop-resumer` (one plain `kubectl delete` of the resumer Job, `--ignore-not-found`) and weight -5 `<release>-self-suspend` (suspends the self `HelmRelease` if present, removes the hook-written values Secret by its labels) at `pre-delete` — and at `pre-upgrade` too while self-management is off (the hand-back and lab renders). They run as the new namespaced identity `<release>-self` (ServiceAccount + Role + RoleBinding, `templates/self/rbac.yaml`, regular objects: the detached resumer runs after a hook-managed account is gone), as do the values hook and the resumer. `gitops.hooks.helmImage` (`docker.io/alpine/k8s:1.36.4` — kubectl, helm, jq, a shell; Renovate-tracked like `gitops.hooks.image`) runs the hooks that need helm or have to tolerate a missing object; `templates/hooks/_job.tpl` takes `script`, `image` and `serviceAccountName`.
- **Lab shape**: `gitops.self.enabled: false` renders none of it (no self objects, no values hook, no policy) but the -6/-5 hooks and their identity; `tests/test-values.yaml` sets it — the ATS installs an unreleased chart, which a self `HelmRelease` would replace with the published one — and `tests/ats/test_smoke.py` asserts the lab shape on the cluster. `make verify-self` (`tests/verify-self.py`, in CI with Helm 3.17.3) asserts the three shapes, the guards and the knobs; `make verify-engine` renders its engine-on shape with self-management off. **No installation needs to act**: the fleet (engine off) renders byte-identically; an engine-on CLI installation becomes self-managed on its next `helm upgrade` — see UPGRADE.md.
- **The chart brings its own Flux engine** (roadmap#4348, PRD Q1/Q2/Q3/Q9): `helm install` of the meta-package on a cluster without Flux yields a running platform. The new `flux-engine` subchart (`helm/agent-platform/charts/flux-engine`, released with this chart, the only `dependencies:` entry of `Chart.yaml`, `condition: components.flux.enabled`, default `true`) carries the seven Flux CRDs of source-controller and helm-controller and the four Flux Operator CRDs in its `crds/`, and renders the Flux Operator (`ghcr.io/controlplaneio-fluxcd/flux-operator` 0.59.0 as a Renovate-managed pin, web UI off, no network policy — the upstream chart's Deployment/ServiceAccount/cluster-admin binding/metrics Service, lifted verbatim) and one `FluxInstance` named `flux` in the release namespace (`distribution.version: "2.x"` from `ghcr.io/fluxcd`, `components: [source-controller, helm-controller]`, `cluster.multitenant: true` with `tenantDefaultServiceAccount: default`, no network policy). The operator upgrades Flux — controllers, CRDs, stored objects — inside the range on its own. Its values live under `flux-engine:`. `components.flux` is a feature switch, not a component: an entry without a `chart` key takes part in the roster and the enablement logic but renders no release (`templates/components.yaml`). Measured on a fresh kind cluster: `helm install --wait` deployed in about two minutes, the `FluxInstance` Ready at Flux v2.9.5 in 15 s.
- **The tenant identity `agent-platform-flux`**: a ServiceAccount in the release namespace bound to `cluster-admin`, rendered by the subchart; `gitops.serviceAccountName` defaults to it whenever the engine is on, so every platform `HelmRelease` names it (under the lockdown a `HelmRelease` without one runs as the rights-less default account and fails). The fleet keeps `""` and its exempt namespace and renders none.
- **Render guards for the engine** (`agent-platform.validateEngine`): with the engine on, a cluster that already runs Flux — a helm-controller `Deployment` (`app.kubernetes.io/component=helm-controller`) or a `FluxInstance` outside the release namespace, looked up live under the Helm CLI, `--dry-run=server` and helm-controller — fails the render with `this cluster runs Flux; set components.flux.enabled=false or install the chart through it`; `gitops.namespace` set to another namespace fails (the tenant identity lives in the release namespace); with the engine off, turning it off on an installation that runs it fails (an upgrade would delete the operator together with the `FluxInstance` it finalizes and hang). `helm template` sees no cluster and renders the engine.
- **The ordered teardown**: pre-delete hook Jobs (`templates/hooks/`, rendered only with the engine on) — a hook ServiceAccount + ClusterRoleBinding `<release>-hooks` to `cluster-admin` at weight -10, `<release>-teardown-releases` at weight 0 deleting the platform `HelmRelease`s by name in `gitops.namespace` and waiting, `<release>-teardown-engine` at weight 5 deleting the `FluxInstance` and waiting; one plain `kubectl` command each in the distroless `registry.k8s.io/kubectl` (`gitops.hooks.image`, a Renovate-managed pin), restricted pod security, `--ignore-not-found`, `hook-delete-policy: before-hook-creation,hook-succeeded`; weights -6/-5 are reserved for the self-management hooks. `helm uninstall --wait` returns clean in well under a minute; the operator removes Flux including its CRDs (every `HelmRelease` object with them, the agents' included; workloads and Helm storage stay), the four operator CRDs remain, a reinstall is clean. Without the hooks Helm deletes the operator together with the `FluxInstance` and hangs for its timeout (measured).
- **A kind install smoke in the ATS** (`.ats/main.yaml` runs the smoke step with `app-tests-skip-app-deploy: true`; `tests/ats/test_smoke.py`): the Gateway API CRDs, then `helm install --wait` of the candidate with `tests/test-values.yaml` (the kind shape: engine on, muster + dicebear + connectivity, valkey off, muster's OAuth server and the network policies off — the `auto` knobs resolve the rest), asserting the release `deployed`, the `FluxInstance` Ready at a Flux 2.x, every component `HelmRelease` Ready as `agent-platform-flux`, one Helm revision, the operator as field manager of the Flux CRDs; then `helm uninstall --wait` in under a minute with no Flux CRD left and the operator CRDs kept, and a clean reinstall. `execute-chart-tests` runs it on every PR.
- `make verify-engine` (`tests/verify-engine.py`, in CI with Helm 3.17.3): engine off renders no CRD, no hook, no operator, no `FluxInstance`, no identity and no `serviceAccountName`, the fleet shape stays clean; engine on renders exactly the eleven CRDs, the operator objects, the `FluxInstance` (two controllers, multitenant), the identities and the hooks at their weights, every `HelmRelease` names `agent-platform-flux`, the teardown Job names every rendered `HelmRelease`; the two shapes' `OCIRepository`/`HelmRelease` documents are identical but for `serviceAccountName` and the roster; `Chart.yaml`'s only dependency is `flux-engine`, conditional, a local directory, its version and `appVersion` in step with the subchart. `make verify-meta` asserts the pure-renderer rule with the engine off and that the one dependency is not a component pin. README: a quick start (domain, identity, components — nothing about Flux), the engine, clusters that run Flux, uninstalling, Helm versions. **Management clusters need not act**: they carry `components.flux.enabled: false` already (management-cluster-bases#721), and the fleet render with it is byte-identical. Other installations that run their own Flux set the value before upgrading — see UPGRADE.md.
- The standalone umbrella's wiring, ported into the connectivity chart and gated on the component toggles the meta chart forwards (roadmap#4348, PRD Q6): with `components.backstage` on, the portal's Agent Platform app-config ConfigMap `agent-platform-backstage-app-config` (the login provider from `global.identity`, one installation `backstage.installationName`, the in-cluster Kubernetes and muster entries, `agentPlatform.fluxServiceAccountName` from the one `kagent.fluxServiceAccountName` value, the skill repositories, the kagent / model-manager API routes when on, the pg block for `backstage.database.engine: postgresql`), the public `HTTPRoute` `backstage.<domain>` and a post-install/post-upgrade config-reload hook that stamps the rendered app-config's checksum into the Backstage Deployment's pod template (`kubectl set env` by label: a no-op while unchanged, a rolling update on a change, nothing on a first install) with its network policy in both flavors (`templates/backstage/`); with `components.mcp-kubernetes` on and muster on, the `MCPServer` CR that registers the server with muster — OAuth, `forwardToken`, `requiredAudiences: [mcp-kubernetes.kubernetesAudience]`, the `infrastructure` tool-group label (`templates/mcp-kubernetes/`); with the new feature switch `components.modelServing` on (an entry without `chart:`: the loop renders no release for it and forwards its flag like every other; a top-level block named after a switch — `modelServing:` — travels with `forwardAllValues` only while the switch is on, so a live connectivity chart that predates the block never sees it), model serving on KServe with vLLM from the `modelServing:` block — the `ClusterServingRuntime`, the seven shipped serving presets and their discovery ConfigMap, the Hugging Face cache PVC, the Kyverno cache policies (`modelServing.policies.enabled: auto`, resolved with the other cluster-shape knobs) and the serving namespace's network policies in both flavors (`templates/model-serving/`, `files/model-serving/`), plus the guard that a model-manager on the `kserve` backend agrees with the layer; the render fails with a clear message when `components.modelServing` is on without `components.kserve-crd` and `components.kserve-resources` (or a cluster that serves the `serving.kserve.io` APIs; `modelServing.kserve.requireApi: false` skips the check); and with `components.kserve-resources` / `components.kserve-llmisvc-resources` on, the controllers' network policies and guards (`templates/kserve/`). The wiring's own keys keep the standalone's names minus `components.` — `backstage.<hostname, parentRefs, installationName, extraScopes, startUrlSearchParams, enabledExtensions, disabledExtensions, skillsRepositories, catalogs, configReload>` and `mcp-kubernetes.kubernetesAudience` live in the chart blocks and are dropped from the values forwarded to the strict component charts (`components.backstage.omitKeys`, `components.mcp-kubernetes.omitKeys`); `modelServing.*` is the standalone's `components.modelServing.*` one-to-one; `components.kserve.*` maps onto the four kserve component toggles (README, "Turning on the standalone's extras"). The seven values blocks of the extras are no longer held back from the connectivity release (`components.agent-platform-connectivity.omitKeys` is empty), the connectivity release `dependsOn` `muster` (the `MCPServer` CRD) and `backstage` `dependsOn` `agent-platform-connectivity` (its pod mounts the ConfigMap rendered there). The kagent UI route guard from the standalone lands too: a `kagent.uiRoute.hostname` override with an oauth2-proxy `redirect-url` still derived from `global.domain` fails the render. `global.identity.ca` (`secretName`, `key`) is declared by the connectivity chart and accepted by the meta chart (`global.identity` is `additionalProperties: true` there — no default, so the `global` block injected into every component release is unchanged). `make verify-wiring` (in CI) asserts off = no object, the objects and guards on, both flavors and the vanilla shape, and the meta chart's forwarding. **No installation needs to act**: every toggle is off on the fleet; the connectivity `HelmRelease`'s values gain the seven blocks and the `modelServing` roster line, its `dependsOn` gains `muster`, and the connectivity chart renders byte-identical objects from them — one Helm revision of that release, no object change, no pod rolled.
- Seven components, off by default — the extras the [agent-platform-standalone](https://github.com/giantswarm/agent-platform-standalone) umbrella carried as Helm dependencies on top of this roster, so a cluster with none of them turns them on from the same `components:` map: `components.backstage` (the Giant Swarm Backstage portal, `0.x`), `components.mcp-kubernetes` (`>=1.1.1 <2.0.0`, the `global.identity` fallbacks its block relies on), `components.cloudnative-pg` (the upstream CloudNativePG operator from `oci://ghcr.io/cloudnative-pg/charts`, `0.29.x` — one chart minor is one operator line and an operator upgrade rolls every instance pod, so moving it is a deliberate edit) and the four KServe charts `components.kserve-crd`, `components.kserve-resources`, `components.kserve-llmisvc-crd`, `components.kserve-llmisvc-resources` (`0.2.x`, one repository, moving together). Each is rendered by the existing loop as one `OCIRepository` + `HelmRelease`; the new top-level blocks `backstage:`, `mcp-kubernetes:`, `cloudnative-pg:`, `kserve-crd:`, `kserve-resources:`, `kserve-llmisvc-crd:`, `kserve-llmisvc-resources:` carry the standalone's defaults (Backstage's chart Ingress and VPA off, the base config restated, the Agent Platform app-config mounted from `agent-platform-backstage-app-config`, the credentials Secret and optional identity-provider CA from `global.identity`; mcp-kubernetes as an OAuth 2.1 resource server acting as the user with downstream OAuth; KServe in Standard deployment mode without per-model routes; the llm-d controller reusing the shared objects) and are forwarded verbatim with `global` injected. `dependsOn` replaces the standalone's two-phase first-install guard: `kserve-resources` after `kserve-crd`, `kserve-llmisvc-resources` after `kserve-crd`, `kserve-llmisvc-crd` and `kserve-resources`, `backstage` after `cloudnative-pg`, and the CR consumers `agent-platform-connectivity` (the CNPG `Cluster`, the model serving objects) after `cloudnative-pg` and `kserve-resources`, `model-manager` after `kserve-resources` — every reference dropped at render time while the component is off, as before. None of the seven ships a `crds/` dir (kserve-crd, kserve-llmisvc-crd and cloudnative-pg render their CRDs as templates, upgraded with the release), so none carries a `crds:` policy. `kserve-llmisvc-crd` is a component the standalone could not have: its 4.5 MB of CRDs did not fit in the umbrella's one release Secret; as its own release it does. `examples/customer-bom.yaml` pins all seven (the standalone's tested pins). The roster forwarded to the connectivity release carries the seven flags, so the wiring the standalone rendered by hand for them (the Backstage app-config and route, the mcp-kubernetes `MCPServer`, the model serving runtimes) can gate on them when it moves into the connectivity chart in a following release; the connectivity chart declares the seven roster entries and the seven blocks already. `make verify-components` asserts the roster (off by default, sources, ranges, order, BOM pins, the forwarded tree against the connectivity schema, schema symmetry between the two charts); `make verify-components-charts` pulls the seven charts at the range's resolution and at the BOM pin and renders each with the forwarded values. **No installation needs to act**: the fleet render changes by the seven `<name>: {enabled: false}` roster lines inside the connectivity `HelmRelease`'s values only — one Helm revision of that release with unchanged objects and no pod rolled.
- `components.<name>.omitKeys` applies to a `forwardAllValues` component too, dropping top-level blocks from the forwarded tree. `components.agent-platform-connectivity.omitKeys` holds the seven new blocks back from the connectivity release until its wiring reads them: that chart's root schema is `additionalProperties: false`, and while both charts release off one tag, the fleet's connectivity release re-resolves its chart on its own `OCIRepository` interval — a block the live chart does not declare would fail that release on every installation until the chart caught up. The release that adds the wiring drops the blocks from the list; the connectivity `values.yaml` declares them from this release on, so that step has no such window.
- The cluster-shape knobs default to `auto` and render by served API group (roadmap#4348, PRD Q4): `kyvernoPolicies.enabled` renders the Kyverno objects when `kyverno.io/v1` is served; `networkPolicy.flavor` is `cilium` when `cilium.io/v2` is served, else `kubernetes`; `global.observability.metrics.serviceMonitor.enabled` renders the monitor objects when `monitoring.coreos.com/v1` is served; `dicebear.route.enabled` (an Envoy `HTTPRouteFilter` user) follows `gateway.envoyproxy.io/v1alpha1`; `agentSandbox.podSecurity.enabled` (a Kyverno mutate policy) follows the resolved `kyvernoPolicies.enabled`. The meta chart detects once, from `.Capabilities.APIVersions` (live under helm-controller, the Helm CLI and `--dry-run=server`; Helm's built-in set under `helm template`, so an offline render without `--api-versions` yields the vanilla shape), resolves every knob before it inlines a component's values, and derives the component-level copies from the same answers while they are left at `auto`: `muster.networkPolicy.flavor`, `muster.muster.observability.metrics.prometheus.serviceMonitor.enabled` / `.prometheusRule.enabled`, `muster.muster.observability.grafanaDashboard.enabled`, `valkey.ciliumNetworkPolicy.enabled`, `valkey.valkey.metrics.podMonitor.enabled` (written `false` only where monitors are off), `kagent.otel.tracing.enabled` / `.logging.enabled`, `kagent.oauth2-proxy.metrics.serviceMonitor.enabled` and the `OTEL_EXPORTER_OTLP_HEADERS` entry of `kagent.controller.env` (dropped when both OTel exporters are off). The connectivity chart carries the same defaults and the same resolution helpers (`agent-platform.shape.*`) for renders on its own; under the meta chart it receives resolved values, so the two cannot disagree on one cluster. Explicit `true` / `false` (`cilium` / `kubernetes`) values keep winning, on a knob and on a component copy; the schema accepts `auto|true|false` (`auto|cilium|kubernetes`) and rejects anything else; the connectivity chart's `.Capabilities` guards for the Flux and KServe APIs stay hard failures. `make verify-auto` (in CI) asserts the resolution per shape, the byte-identical fleet render, the vanilla shape and the overrides. **No installation needs to act**: with the fleet's API groups served the render is byte-identical to before; a kind or plain cloud cluster renders the vanilla shape from the same chart without a values file that describes it.
- `kagent.fluxServiceAccountName` (default `kagent-flux`), in both charts: the tenant identity of the agents' Flux `HelmRelease`s, from ONE value. The connectivity chart renders ServiceAccount `kagent-flux` in the kagent namespace with a RoleBinding to `cluster-admin` (namespace-scoped admin: full control of the kagent namespace, nothing outside it) whenever kagent is on (`templates/kagent/flux-service-account.yaml`); the meta chart derives agent-manager's `flux.helmReleaseServiceAccount` from the same value (`agent-platform.componentDerivedValues`, merged over the forwarded `agent-manager` block — `agent-manager.flux.helmReleaseServiceAccount` is no longer set in `values.yaml`, and a value set there must agree or the render fails naming the one key); and the `agent-platform.kagent.fluxServiceAccountName` helper exports it for the portal's `agentPlatform.fluxServiceAccountName`. Changing it in one place changes all three; an empty value renders no identity and hands both callers an empty name. Under a Flux multi-tenancy lockdown (helm-controller with `--no-cross-namespace-refs` and a rights-less default ServiceAccount, or the Flux multi-tenancy admission policy on Giant Swarm management clusters) a `HelmRelease` without it runs as the default ServiceAccount and fails with `secrets is forbidden`. `make verify-identity` (in CI) asserts the objects, the single value across the three consumers, the guard and the empty case. **No installation needs to act**: a management cluster carried the same two objects hand-written in its Flux bases; the chart's copy has the same spec, helm-controller takes ownership of them on the upgrade (its default) and both writers set the same fields. The hand-written copy is removed from the bases in a follow-up.
- `muster.muster.toolsetPresets`: the platform's two toolset presets, forwarded into muster's config. `infrastructure` selects every `MCPServer` labelled `agent-platform.giantswarm.io/tool-group=infrastructure` (the mcp-kubernetes, mcp-capi and mcp-prometheus families, stamped by agent-platform-mcps ≥ 0.9.0); `agent-platform` selects the `agent-platform` label (agent-manager ≥ 0.3.0, model-manager ≥ 0.18.0) plus muster's `core_*` tools — the meta agent's preset. Agents name them as `preset:infrastructure` / `preset:agent-platform` in their `toolset`; muster resolves the label live on every request, so a new manager or infrastructure family joins its preset with no values change. Installations add their own next to them (Helm merges the map). A preset named like a built-in (`read-only`, `none`, `full`) fails the meta render, naming it, instead of failing the muster pod's start out of sight. Documented in [docs/toolset-presets.md](./docs/toolset-presets.md); `make verify-presets` asserts the presets on the muster HelmRelease (both engines), pulls the muster chart at the range floor and renders its ConfigMap from the forwarded values.
- `model-manager.lmstudio.endpoint` / `.agentHost` and the `lmstudio` backend in the connectivity chart's model-manager wiring: an LM Studio on the host (llama.cpp on GPU and CPU, MLX on Apple silicon) as a serving backend. `templates/model-manager/validate.yaml` accepts the name and, as for ollama and lemonade, fails the render when the endpoint is missing or carries no scheme; `_helpers.tpl` gains `modelManager.lmstudioEndpoint` and the `lmstudio` arms of `hostEndpoint`/`hostTargets`, so the model-manager NetworkPolicy opens egress to the LM Studio host in both flavors. **Requires model-manager >= 0.19.0**, and that is a pin to move rather than a backend to select: `values.yaml` forwards the whole `model-manager` block — `lmstudio` included — to every release, and the key does not exist before 0.19.0, whose schema is `additionalProperties: false`, so a chart pinned below it rejects the values outright even on an installation that never touches LM Studio (`examples/customer-bom.yaml` floors it accordingly). LM Studio serves no delete (`lms rm` is host-only), so that call answers `501 unsupported`.
- `kagent.oauth2ProxyIngress.additionalPeers` (default `[]`), in both charts: extra callers the kagent oauth2-proxy ingress policy admits on the proxy port (4180) next to the public Gateway's Envoy pods — a list of pod label maps (`matchLabels`), each rendered as one more `fromEndpoints` entry (cilium flavor; the kubernetes flavor renders no oauth2-proxy policy). A peer in another namespace carries its own `io.kubernetes.pod.namespace`; an item that is not a non-empty label map fails the render. For an installation whose kagent hostname sits on a private edge LB and whose kagent API is reached from elsewhere through a Teleport tunnel: the local `teleport-kube-agent` (`app: teleport-kube-agent` in `kube-system`) proxies the tunnel's requests to oauth2-proxy and was dropped by the policy, so the tunnel hung without an HTTP answer. `make verify-kagent-netpol` covers it. **No installation needs to act**: the default render is unchanged.
- `gitops.forbidInlineSecrets` (default `false`): with `true` the meta-package render fails when a credential is set inline in the values — `kagent.providers.<name>.apiKey`, `kagent.oauth2-proxy.config.clientSecret` / `.cookieSecret`, `muster.muster.oauth.server.dex.clientSecret` / `.google.clientSecret` / `.registrationToken` / `.encryptionKeyValue` / `.storage.valkey.password`, `valkey.valkey.auth.aclUsers.<user>.password`, `klausGateway.slack.botToken` / `.signingSecret` / `.appToken`, `klausGateway.obo.stateKey` / `.storeKey`, `model-manager.oauth.dex.clientSecret`, `agent-manager.oauth.dex.clientSecret` — and names the offending key paths (never their values). Such a value is forwarded verbatim into the component's HelmRelease `spec.values` (Argo: `valuesObject`) and into Helm's release storage, readable by anyone allowed to get HelmReleases in `gitops.namespace`; every component chart takes the same credential from a pre-created Secret instead (kagent `providers.<name>.apiKeySecretRef` with an empty `apiKey` and `oauth2-proxy.config.existingSecret`, muster `oauth.server.existingSecret` and `.storage.valkey.existingSecret`, valkey `auth.usersExistingSecret`, klaus-gateway `slack.secretName` with an empty `botToken` and `obo.existingSecret` (klaus-gateway >= 0.39.0), model-manager / agent-manager `oauth.existingSecret`). The values.yaml guidance for those keys now recommends the pre-created Secret, and `klausGateway.obo.existingSecret` is documented next to the keys it replaces. `make verify-secrets` (in CI) pins the default, the failure and the referenced-Secret render. **No installation needs to act**: the flag is off and the default render is unchanged; an installation that has moved its credentials into Secrets sets it to `true` so they cannot come back.

### Changed

- The `kagent` component tracks the `0.2.x` chart line, which flattened the upstream keys onto the chart root. The forwarded values block is no longer nested under a `kagent` key, and the umbrella-only keys (`controllerRoute`, `modelConfigs`, `remoteMcpServers`, `serviceMonitor`, `uiRoute`) are dropped before forwarding (`omitKeys`) because the flattened chart's schema rejects them. **The top-level `kagent:` values block is unchanged**, so no installation has to move an override. `examples/customer-bom.yaml` pins `0.2.0`. The kagent controller and UI pods roll once on the upgrade (three labels and the pod annotations that hash them change).
- `components.kagent.versionRange` is `0.1.x` (was `0.x`). kagent 0.2.0 flattens the upstream keys onto the chart root and takes no nested `kagent:` block, so the current wiring must not pick it up. The wiring change and the `0.2.x` range ship together in a later release. **No installation needs to act.** A BOM that pins `components.kagent.versionRange` inside `0.1.x` is unaffected.
- `components.muster.versionRange` floors at 5.12.0 (`>=5.12.0 <6.0.0`, was `5.x`): the muster chart version with the `label:` preset rule. An older muster refuses to start on the shipped presets, so the source must resolve a chart that has the rule before the release applies the values that need it. Minor releases keep rolling forward on their own. BOM-pinned installations pin muster at 5.12.0 or later (UPGRADE.md).
- `examples/customer-bom.yaml` is a fresh snapshot of the internal dogfooding track (muster 5.12.0, agent-platform-mcps 0.9.0, agent-manager 0.4.0, model-manager 0.18.0, agentgateway 2.0.7, kagent 0.1.48, klaus-gateway 0.39.0, agent-sandbox 0.2.31, dicebear 0.3.10, valkey 0.1.4, connectivity 3.10.0); the previous pins predated the chart's current line.
- The CNPG Cluster's CiliumNetworkPolicy (`<clusterName>-cluster`) selects the pods of a Cluster named `<clusterName>-restore` as well — the scratch Cluster a restore test bootstraps from the backup store (`bootstrap.recovery` + `externalClusters[].plugin`). Its full-recovery Job needs the Kubernetes API and the object store, which the CNPG operator's cluster-wide policies (instance, initdb, join pods) do not grant to recovery Jobs; without this the restore Job times out on the API server. Name a scratch restore Cluster exactly `<clusterName>-restore` (the Crossplane IAM role already trusts `<clusterName>-restore*`). **No installation needs to act.**

### Fixed

- Documented in the README: enabling `postgres.backup` on a Cluster that is already running rolls its instance pods to add the plugin sidecar, and the `immediate` first `Backup` fires before the sidecar exists and fails with `requested plugin is not available`; the next scheduled run (or a manual `Backup` with `method: plugin`) succeeds. A Cluster created with the block from the start does not hit this.

### Added

- `postgres.backup.*` in both charts: backups for the CNPG `Cluster` the connectivity chart renders, off by default. `method: plugin` (the default) wires the [Barman Cloud plugin](https://cloudnative-pg.io/plugin-barman-cloud/) — CNPG's supported object-store path since 1.26, the one the in-tree `barmanObjectStore` is deprecated in favour of — as the Cluster's WAL archiver and renders an `ObjectStore` (`<clusterName>-backup`: `destinationPath`, optional `endpointURL` for S3-compatible stores, exactly one credential source out of `s3.inheritFromIAMRole`, static S3 keys, `azure.inheritFromAzureAD`, an Azure connection string or account+key Secret references, gzip WAL/data compression, `retentionPolicy` default `30d`, sidecar resources) and a `ScheduledBackup` (`<clusterName>-scheduled`, default daily at 02:00 UTC, `immediate`, `backupOwnerReference: self`). `method: volumeSnapshot` renders `backup.volumeSnapshot` on the Cluster instead (no WAL archive). `serverName` overrides the folder in the store, `objectStore.existingName` reuses a store, `serviceAccount.annotations` binds the Cluster's ServiceAccount to a cloud identity (IRSA role, workload identity) for the sidecar. In the cilium flavor the CNPG policy gains egress for the store (DNS, `world` on `networkPolicy.ports`, the `cluster` entity for an in-cluster `endpointURL`, optional `fqdns` and `cidrs`). `postgres.backup.crossplane.*` (off) provisions the store the way the fleet provisions its own object storage: on `aws` an S3 `Bucket` with a lifecycle rule, a public-access block and a TLS-only policy plus an IAM `Role` whose trust policy names the cluster's IRSA OIDC provider for the Cluster's ServiceAccount and `<clusterName>-restore*` (scratch restore clusters), with `destinationPath`, `s3.inheritFromIAMRole` and the role annotation derived; on `azure` a Storage `Account`, a `Container`, a lifecycle `ManagementPolicy` and, on private installations, a `PrivateEndpoint` in the node subnet, with `destinationPath` and `azure.connectionString` derived from the Account's connection Secret (`<clusterName>-backup-store`). Bucket, Account and Container are never deleted by Crossplane and carry `helm.sh/resource-policy: keep`. Guards fail the render on a missing or foreign `destinationPath`, zero or several credential sources, IRSA without a role annotation, a bad `method`, `retentionPolicy` or storage-account name, a snapshot method without a class and missing Crossplane inputs. `make verify-postgres` covers it; `ci/test-postgres-values.yaml` (MinIO shape), `ci/test-postgres-backup-aws-values.yaml` and `ci/test-postgres-backup-azure-values.yaml` render it. **No installation needs to act**, but every installation with `postgres.enabled` and no `postgres.backup` should: its Cluster now carries `agent-platform.giantswarm.io/backup: none` and NOTES warns that the PVCs are the only copy of the platform database. A CNPG Cluster without a backup still reports `ContinuousArchiving=True/ContinuousArchivingSuccess` — the operator's `wal-archive` command exits 0 when there is nowhere to archive to — so that condition is not a signal.
- `modelManager.networkPolicy.egress` (`fqdns`, `cidrs`, both empty): further destinations of model-manager on 443 whatever the backend — a private identity provider, a model mirror — the same knob `agentManager.networkPolicy.egress` already is. cilium: FQDN selectors next to the Hugging Face names and the Ollama endpoint, blocks as `toCIDR`; kubernetes: the blocks as `ipBlock`s (names are ignored, the flavor has no FQDN selector). `networkPolicy.additionalEgressCIDRs` / `additionalEgressFQDNs` now apply to the model-manager cilium egress for the ollama backend too (they used to render only for the kserve backend; the kubernetes flavor always applied them).
- `agent-platform.idpHosts` helper in the connectivity chart: the hosts a service that validates tokens itself reaches at its identity provider, from the OAuth provider and the issuer URL — the Dex issuer host, or Google's `accounts.google.com`, `www.googleapis.com` and `oauth2.googleapis.com`. `agent-platform.idpEgress.cilium` takes `provider` next to `issuerUrl` and renders one `toFQDNs` rule over those hosts.
- Two components, off by default: `components.model-manager` ([model-manager](https://github.com/giantswarm/model-manager), model inventory / pull / load / unload / delete and kagent `ModelConfig` wiring on an Ollama or KServe backend) and `components.agent-manager` ([agent-manager](https://github.com/giantswarm/agent-manager), the agent write surface: kagent agents as Flux `HelmRelease`s of the agent chart), each as REST and MCP through muster's own `MCPServer` CR and each acting as the user (OAuth 2.1 resource server, forwarded id_tokens, downstream OAuth to the Kubernetes API, no ServiceAccount RBAC). The `model-manager:` / `agent-manager:` blocks are forwarded to the component releases; the `modelManager:` / `agentManager:` blocks drive the wiring the connectivity chart renders for them — the agentgateway route and JWT policy (`route.*`, the kagent controller route's shape), the network policies in both flavors (ingress from the data plane, muster and the kubelet's probes; egress to DNS, the Kubernetes API, the identity provider, the Ollama endpoint or the Hugging Face Hub, the agent chart's registry and GitHub) and render-time guards (backend, endpoint, KServe and Flux APIs, kagent on and same namespace, muster for the MCPServer CR, the OAuth inputs, route mode / hostname / parentRef / JWKS egress). `make verify-managers` covers them; `examples/customer-bom.yaml` pins both. **No installation needs to act**: both components are off and the default render is unchanged.
- `modelManager.networkPolicy.ingress.additionalPeers` and `agentManager.networkPolicy.ingress.additionalPeers` (default `[]`), in both charts: extra callers the model-manager / agent-manager ingress policies admit on the Service port next to the agentgateway data plane and muster — a list of pod label maps (`matchLabels`) in the release namespace, rendered as `fromEndpoints` entries with `io.kubernetes.pod.namespace` pinned to the release namespace (cilium) or as `podSelector` entries (kubernetes). A non-empty list counts as a platform caller, so the no-caller placeholder peer does not render next to it; an item that is not a non-empty label map fails the render. For an umbrella that runs a portal next to the managers (the standalone chart's Backstage, which patched the peer into its generated copies until now). `make verify-managers` covers it. **No installation needs to act**: the default render is unchanged.
- `agent-platform.dnsEgressWithProxy`, `agent-platform.urlHost` and `agent-platform.idpEgress.cilium` helpers in the connectivity chart: a DNS egress rule with Cilium's proxy clause for policies that select destinations by name, and the egress a platform service needs to reach the login identity provider (by name on 443, plus the cluster entity on 443/10443 for an issuer behind an in-cluster Gateway).
- The `global.*` input contract, in both charts, with backward-compatible fallbacks — **an installation that sets none of the new keys renders byte-identically** (the connectivity golden check pins this). Upstreamed from the standalone chart's vanilla-defaults work so the generated standalone templates need no patches:
  - `global.domain`: the one hostname input. Public hostnames derive from it by convention (`muster.`, `agentgateway.`, `kagent.<domain>`), each still overridable next to its route (`ingress.hostnames`, `kagent.uiRoute.hostname`, `kagent.controllerRoute.hostname`). The kagent route guards now accept a derived hostname: with `global.domain` set, `kagent.uiRoute.hostname` is optional.
  - `global.identity` (`issuerUrl`, `clientId`, `existingSecret`): the platform's one login provider. The kagent JWT policy defaults its `issuer` from `global.identity.issuerUrl`; the render fails when muster's own `muster.muster.oauth.server.*` keys disagree with a set `global.identity` (both sides set and different — installs that ignore `global.identity` are untouched).
  - `global.gatewayApi.parentRefs`: the public Gateway every umbrella-owned route attaches to when neither its own `parentRefs`/`parentRef` override nor the chart-owned edge names one. The all-modes `ingress.parentRefs` requirement is now satisfied by any of the three sources; the failure message names them all.
  - `global.observability.metrics.serviceMonitor` (`enabled` default `true`, `interval`, `labels`): gates the monitor objects the connectivity chart renders — the kagent controller ServiceMonitor (+ its metrics Service) and the CNPG Cluster's built-in PodMonitor — for clusters without the `monitoring.coreos.com` CRDs. The per-component `kagent.serviceMonitor.*` keys keep working underneath; `interval`/`labels` fall back to them.
  - `global.observability.traces.otlp` (`endpoint`, `protocol`, `headers`): OTEL exporter env for the agentgateway data plane. An empty endpoint renders nothing and `gateway.parameters.dataPlaneEnv` applies unchanged; a set endpoint wins over same-named entries in that list, so no duplicate env names.
- `ingress.httpRoute.timeouts` (default `{}`): portable Gateway API rule timeouts on the umbrella-owned routes (muster `/`, agentgateway `/mcp`, kagent UI and public controller routes). `request: "0s"` asks the implementation to disable the request timeout so MCP/SSE streams stay open without the Envoy-specific `ingress.backendTrafficPolicy`.
- `gatewayApi.gateway.create` (default `false`, with `tls.secretName` and `serviceType`): edge mode — the chart-owned agentgateway data-plane Gateway is also the public edge. It grows an HTTPS `:443` listener for `*.<global.domain>` (`allowedRoutes` from all namespaces), the umbrella-owned public routes pin themselves to that listener via `sectionName` (the plaintext `:8080` listener never serves public hostnames through the edge's LoadBalancer Service), the layer-1 routes that would forward a front Gateway to the agentgateway Service are suppressed (the data plane must not proxy to itself, and their `BackendTrafficPolicy` with them), the data-plane Service type switches to `gatewayApi.gateway.serviceType`, and the data-plane NetworkPolicy (both flavors) admits world traffic on 443 only. Guards require the agentgateway mode, the certificate Secret and `global.domain`. **The fleet never sets this** — it exists for standalone installs (kind, vanilla clusters) without a front Gateway. Known accepted quirk, documented in the muster route template: in edge mode the hostname-specific muster `/` route also serves `muster.<domain>/mcp` directly (hostname precedence), so the MCP path degrades to muster-direct semantics on that hostname; behavioral acceptance is owned by the standalone kind smoke.

- `components.<name>.omitKeys`, the keys dropped from a component's forwarded values block. It exists for a block that holds a key belonging to this umbrella rather than to the component's chart, where that chart validates values with `additionalProperties: false` and would reject it.
- `components.<name>.enabled` on `agent-platform-connectivity`, the same key path the meta chart reads. The meta chart forwards the component roster, stripped to enablement, into the values of that release, so the wiring the chart renders for a component agrees with whether the component has a release. Set it directly only when installing `agent-platform-connectivity` on its own.
- `kyvernoPolicies.enabled` (default `true`) in both charts. Set it to `false` on a cluster that has no Kyverno: the connectivity chart then renders none of its `kyverno.io` objects (the two kagent declarative-agent ClusterPolicies, the kagent seccomp PolicyException, the agent-sandbox pod-security ClusterPolicy), which otherwise fail the install on an unknown API group. **No installation needs to act.** A cluster that enforces restricted PSS through PSA labels instead of Kyverno must set `components.agent-sandbox.enabled: false` as well, or `agentSandbox.podSecurity.enabled: false` where nothing enforces restricted PSS; the render now fails with that instruction instead of shipping a controller Deployment with no securityContext.
- A PolicyException for the CNPG instance pods, and the `kyvernoPolicies.volumeTypesPolicyName` (`restrict-volume-types`) and `kyvernoPolicies.volumeTypesRuleNames` (`restricted-volumes`, `autogen-restricted-volumes`) keys it targets, in both charts. The connectivity chart renders it only when `postgres.vector.extensionImage.reference` is set, the one configuration where a CNPG pod mounts an `image` volume, which is not in that policy's allowed types. Without the exception admission rejects every instance pod, the Cluster reaches no active instance, and the kagent controller crashloops on its pgvector check. The exception matches on the `cnpg.io/cluster` label of `postgres.clusterName`, so it covers that Cluster's pods and the operator's bootstrap Jobs only. **No installation needs to act** unless its cluster names those policies differently.
- `kyvernoPolicies.policyExceptionNamespace` (`policy-exceptions`), `kyvernoPolicies.seccompPolicyName` (`restrict-seccomp-strict`) and `kyvernoPolicies.seccompRuleNames`, the cluster-owned names the seccomp PolicyException targets. They default to the Giant Swarm cluster policy names; a cluster that names its policies differently must override them, or the exception matches nothing.

### Changed

- The CNPG `Cluster` carries `helm.sh/resource-policy: keep`: the database outlives the release, so an uninstall or a `postgres.enabled` flip no longer drops the platform's data. Removing the database now takes an explicit `kubectl delete cluster` after the release change.
- The CNPG Cluster's `inheritedMetadata` labels and `monitoring` block are gated on `global.observability.metrics.serviceMonitor.enabled` (default `true` — no installation needs to act), and the labels come from `global.observability.metrics.serviceMonitor.labels`, defaulting to the Giant Swarm tenant label as before.
- The `valkey` component ships `deploymentStrategy: Recreate`. The single pod owns an RWO PVC, so under `RollingUpdate` every values or chart change deadlocked the rollout: with one replica `maxUnavailable` is 0, the new pod waited on `Multi-Attach` for a volume the old pod held, and Helm timed out after 5 minutes. An installation that pinned `valkey.valkey.deploymentStrategy` keeps its own value. The store is a cache, so the restart gap between the two pods costs nothing.
- The `agentgateway` component tracks the `2.x` chart line, which flattened the upstream keys onto the chart root. The forwarded values block is no longer nested under an `agentgateway` key. **The top-level `agentgateway:` values block otherwise keeps its shape**, so only the toggle moves. `examples/customer-bom.yaml` pins `2.0.0`.
- `kyvernoPolicies.seccompRuleNames` must hold at least one non-empty name, and the connectivity chart fails the render when `agentSandbox.podSecurity.enabled` is on without `kyvernoPolicies.enabled`. Both configurations used to render an object that silently matched nothing.
- The agent-sandbox pod-security ClusterPolicy no longer carries `helm.sh/resource-policy: keep`. Helm prunes it now whenever it stops rendering (`kyvernoPolicies.enabled: false`, `agentSandbox.podSecurity.enabled: false`, or the planned v0.5.0 removal of the template), instead of leaving an orphan ClusterPolicy that has to be deleted by hand.
- The `agent-sandbox` component `dependsOn` the `agent-platform-connectivity` release, so Kyverno holds the mutate policy that carries the controller Deployment's securityContext before the Deployment applies. The agent-sandbox release no longer fails its first install on a restricted-PSS cluster and then converges on a retry. The window is narrowed, not closed: `dependsOn` waits for the ClusterPolicy to apply, not for the Kyverno webhook to serve it, so the release retries still cover the rest. The agent-sandbox release now also waits on the whole connectivity release, so a connectivity failure holds it back. Flux only: the argo engine has no `dependsOn`, and its sync waves do not express this order yet.

### Removed

- The six per-chart component toggles: `agentgateway.enabled`, `valkey.enabled`, `mcps.enabled`, `kagent.enabled`, `klausGateway.enabled` and `agentSandbox.enabled`. `components.<name>.enabled` is now the only on/off switch for a component, and every installation that sets one of the old keys must move it — see [UPGRADE.md](./UPGRADE.md) for the key-by-key move. `components.<name>.enabled` already existed and already won over the old `enabledFrom` indirection, so a component could be disabled two ways and the two answers could disagree: `components.agentgateway.enabled: false` with `agentgateway.enabled: true` rendered no agentgateway release but still rendered that controller's ListenerSet ClusterRole.
- `components.<name>.enabledFrom`, the indirection from a component entry to a per-chart toggle. There is nothing left to point at.
- Both charts fail the render when one of the removed keys is still set, and name its replacement. Those value blocks accept unknown keys, so an ignored `enabled` would silently turn the component off.
- The `mcps:` values block. It only ever held the toggle; the chart's values keep living under `agent-platform-mcps:`.

### Fixed

- The kagent cilium network policies open egress from the kagent controller and from the kagent-managed agent pods to kagent's built-in tool server when it is enabled (`kagent.kagent-tools.enabled: true`): the `app.kubernetes.io/name: kagent-tools` pods in the tools namespace on the tools port (8084; an explicit `kagent.kagent-tools.namespaceOverride` / `service.ports.tools.targetPort` is honoured). `kagent-controller-egress` and `kagent-agent-muster-egress` are default-deny egress lists, so the controller's tool discovery (MCP initialize + tools/list on the `kagent-tool-server` RemoteMCPServer URL) and the agents' tool calls were dropped as `Policy denied`: the RemoteMCPServer stayed `Accepted=False` and every agent that referenced it ran without tools. Nothing renders for installations with the tool server off (the default). `make verify-kagent-netpol` (in CI) pins the rules both ways.
- The model-manager and agent-manager cilium network policies open egress to Google when the component validates tokens with `oauth.provider: google`. The IdP egress rule named the issuer host by FQDN and the per-component issuer helpers deliberately return nothing for the google provider, so a Google-backed install rendered no name at all: mcp-oauth's JWKS fetch (`www.googleapis.com/oauth2/v3/certs`), userinfo fallback and discovery (`accounts.google.com`) timed out under `policyEnforcementMode: always`, muster's forwarded-token sessions failed and the operator had to add the three hosts to `agentManager.networkPolicy.egress.fqdns` and open `world:443` for model-manager by hand (agent-platform-standalone#106). The rule now derives the hosts from the provider: `accounts.google.com` (discovery, authorization), `www.googleapis.com` (JWKS, userinfo) and `oauth2.googleapis.com` (token, revocation) — the endpoints mcp-oauth's google provider dials — next to the unchanged cluster entity on 443/10443. The dex render is byte-identical; the kubernetes flavor already opened every public destination on 443 while OAuth is on. `make verify-managers` asserts both providers and the new egress knob.
- `AgentgatewayParameters` renders the Service-type overlay at `spec.service.spec.type`, the path the CRD declares (an embedded `ServiceSpec` under `spec.service.spec`), instead of `spec.service.type`, which is not in the schema. Until now the overlay never took effect: helm-controller's client-side apply silently pruned the unknown field, so the agentgateway data-plane Service stayed at the controller-hardcoded `LoadBalancer`; under Helm 4's default server-side apply the same field made the install hard-fail (`.spec.service.type: field not declared in schema`). **Visible rollout effect:** on installations running an `agentgateway-*` ingress mode without the chart-owned edge, the data-plane Service now actually becomes `gateway.parameters.serviceType` (default `ClusterIP`, the intended state — the front Gateway serves public traffic), which **deletes the unintended cloud LoadBalancer** that the pruned overlay left behind. Edge-mode installs (`gatewayApi.gateway.create: true`) keep `LoadBalancer` via `gatewayApi.gateway.serviceType`. Fixes [#250](https://github.com/giantswarm/agent-platform/issues/250).
- The legacy-toggle guard (`validateLegacyToggles`, both charts) is coalescing-safe. In a layout where a component block feeds a real Helm dependency (the standalone umbrella copies this helper), Helm coalesces that chart's own top-level `enabled: true` default into the block once the dependency is on (klaus-gateway ships one), so the plain `hasKey` probe could not tell an operator-set value from the chart default and was skipped exactly where it mattered. The guard now reports a legacy key when it is provably the operator's: always while the component is off, and while it is on when the value is an explicit `false` — the case where the operator believes the component is off while it runs anyway. A leftover `enabled: true` under a component that is on is indistinguishable from a coalesced chart default and passes.
- The CNPG cluster CiliumNetworkPolicy (`<clusterName>-cluster`: replication egress, controller ingress) is gated on `postgres.enabled`. It used to render for every kagent install under the cilium flavor, naming a Postgres cluster that does not exist.
- The kagent `uiRoute`/`controllerRoute` `parentRef` overrides fail the render when `.name` is set without `.namespace` — the Gateway API CRD rejects an empty namespace, which used to break the route silently after install.
- The `kagent-pg` CNPG Cluster now sets `inheritedMetadata.labels` with
  `observability.giantswarm.io/tenant: giantswarm`, which the operator copies
  onto the PodMonitor it generates. Without it the PodMonitor carried only
  CNPG's stock labels and matched neither of alloy-metrics' discovery
  selectors, so it was created and silently never scraped (zero `cnpg_*`
  series in Mimir). The previous `podMonitorRelabelings` attempt is removed:
  it rewrote scraped series, not the PodMonitor object's labels, so it could
  never make the monitor discoverable.

### Changed

- `components.muster.versionRange` default changed from `">=4.1.0 <5.0.0"` to `"5.x"`, so the meta-package resolves the current muster line (`v5.1.0`). The `>=4.1.0` floor is subsumed by the major: it existed because the muster values here set `observability.metrics.prometheus.prometheusRule`, and every `5.x` accepts that key. **No installation needs to act.** muster `v5.0.0` ([muster#1078](https://github.com/giantswarm/muster/pull/1078)) is breaking only for muster instances running *without* their own OAuth: with no bearer there was no token family, so the transport injected the `default-user` placeholder and every concurrent MCP client collapsed onto one session — one client's authenticated backend became visible and callable in every other client's session, on the first client's token. Such sessions are now keyed by MCP transport session, which means a session ends with the connection and a reconnecting client re-runs `core_auth_login`. Every installation of this umbrella runs `muster.muster.oauth.server.enabled: true` (the default here, overridden in no `*-configs` installation patch across the 14 deployments), so a real token-family session ID still wins and the crossing is a no-op for the fleet; stdio is unaffected either way. muster `v5.1.0` ([muster#1079](https://github.com/giantswarm/muster/pull/1079)) additionally fails the render on an explicitly emptied `rbac.mcpServerEditor.subjects` / `rbac.workflowEditor.subjects`; both are bound to the Giant Swarm admin groups by this chart (since #232) and overridden nowhere, so the guard is satisfied. The crossing also picks up [muster#1077](https://github.com/giantswarm/muster/pull/1077) (the chart values-schema fix), which the never-published `v4.1.1` chart left unreachable.

- `components.muster.versionRange` default changed from `"3.x"` to `"4.x"`. muster `v4.0.0` ([muster#1075](https://github.com/giantswarm/muster/pull/1075), [muster#1069](https://github.com/giantswarm/muster/issues/1069)) routes Workflow spec mutations (`core_workflow_create`/`_update`/`_delete`) through the same caller-identity gate MCPServer writes got in `v2.0.0`: in Kubernetes mode the write carries the session's own dex id_token, authorized by k8s RBAC via the chart-shipped `workflow-editor` role (bound to all authenticated users by default, mirroring `mcpserver-editor`). **No installation needs to act:** sessions already must carry the dex-k8s-authenticator audience for MCPServer writes since the `2.x` crossing (same gate, same token), the `workflow-editor` Role/RoleBinding ships in the muster chart itself, and no fleet installation overrides `rbac.mcpServerEditor` or disables muster `rbac.create` (checked via org-wide code search). The crossing also *fixes* `core_workflow_update` on Kubernetes installations — the old path sent an update without a `resourceVersion`, which the apiserver rejects for CRs, so the tool never worked there.

### Removed

- The deprecated `agentic-platform-mcps` values key. Overrides must live under `agent-platform-mcps`. The `legacyValuesFrom` component field that carried the fallback is gone too, along with the free-form `agentic-platform-mcps` property in both `values.schema.json` files: setting the old key is now a schema error rather than a silent override.

### Changed

- Charts renamed: `agentic-platform` is now `agent-platform` and `agentic-platform-connectivity` is now `agent-platform-connectivity`. The published OCI paths change to `oci://gsoci.azurecr.io/charts/giantswarm/agent-platform` and `.../agent-platform-connectivity`; the last release under the old names is 2.5.4. The GitHub repository is now `giantswarm/agent-platform`.
- The mcps values key is renamed from `agentic-platform-mcps` to `agent-platform-mcps`, following the `agent-platform-mcps` chart rename (first new-name release 0.7.0, still within the `0.x` range).
- Namespace-derived defaults now assume the `agent-platform` namespace: `agent-platform-mcps.muster.musterUrl` and `kagent.a2a.url`. Deployments in a different namespace must override them (unchanged requirement, new default value).
- The default Postgres cluster name (when `postgres.clusterName` is unset) is `agent-platform-pg` instead of `agentic-platform-pg`.

### Fixed

- The agentgateway component now resolves from the `1.x` chart line. The wrapper chart left `0.x` at `0.1.0`; every release since is `1.0.x`, so the `0.x` range held the component on a chart that vendors a `v1.3.1` controller. `1.0.11` is the first release that vendors `v1.4.1`.
- The agentgateway controller and proxy image tags are no longer pinned here. Both now come from the `agentgateway` wrapper chart: the controller from the vendored subchart's appVersion (`v1.4.1`), the proxy from that chart's own `proxy.image.tag`. The `gsoci.azurecr.io` registry overrides stay. Gateway API `1.6` (shipped by `gateway-api-bundle` `1.19.0`) serves `TCPRoute` at `v1` only, and `v1.2.1` watches `v1alpha2`: its `TCPRoutes` informer never syncs, `/readyz` never answers, and the install fails the startup probe.
- kagent images are no longer held at `0.9.9`. `kagent.tag: "0.9.9"` was still pinned in the meta-package values, overriding the wrapper chart and keeping `kagent-controller` and `kagent-ui` on `0.9.9` even though `kagent-app` has vendored upstream `0.9.12` since chart `0.1.32`. The pin was a workaround for pulling the upstream chart directly via `OCIRepository`, where helm-controller appended the OCI digest to the chart version and `+` is illegal in an image tag; now that `kagent-app` vendors the upstream chart as a subchart, its `.Chart.Version` stays clean and the upstream tag coalesce resolves correctly on its own.
- The example customer BOM pinned `kagent` to `versionRange: "0.9.9"`, a version the wrapper chart never publishes (`kagent-app` ships `0.1.x`), so the example could not resolve. Pinned to `0.1.33`.
- muster metrics are now actually scraped. Both charts (umbrella + connectivity) passed `serviceMonitor.enabled: true` at the top level of the muster values — a key the muster chart never had, so it was silently ignored: no `/metrics` endpoint, no metrics port, no ServiceMonitor on any installation. The toggle now lives at the real path, `muster.observability.metrics.prometheus.serviceMonitor.enabled` (muster ≥ `v1.4.0` derives the prometheus exporter from it), with a `60s` interval and the `observability.giantswarm.io/tenant: giantswarm` label so the series land in the giantswarm Mimir tenant.

### Changed

- `klausGateway.*` forwarded sub-blocks in both `values.schema.json` (umbrella + connectivity) relaxed to `additionalProperties: true`: `crd`, `observability`, `slack`, `cli`, `agentgatewayRoute`, `obo`, `obo.persistence`, `obo.connectors`. These mirror klaus-gateway subchart config forwarded via `forwardAllValues`, so the subchart's own schema is the source of truth; the umbrella no longer re-validates them. New klaus-gateway keys stop failing the meta-package HelmRelease schema check before they can be mirrored by hand (the recurring `Additional property ... is not allowed` break, e.g. [#209](https://github.com/giantswarm/agentic-platform/pull/209)/[#210](https://github.com/giantswarm/agentic-platform/pull/210)/[#211](https://github.com/giantswarm/agentic-platform/pull/211)). This aligns `klausGateway` with the other forwarded components (`kagent`, `muster`, `mcps`, `agentgateway`), which are already permissive or undeclared. Meta-package-owned blocks (`postgres`, `gateway`, `ingress`, `networkPolicy`, `global`) stay `additionalProperties: false`.
- `components.muster.versionRange` default changed from `"0.x"` to `"3.x"`. muster crossed three majors inside this unreleased window; the range now resolves the current line. **No installation needs to act for any of them** — the specifics below. First crossing: muster crossed a major with [#968](https://github.com/giantswarm/muster/pull/968) (`feat(oauth)!: remove JWT mode`) — muster no longer signs tokens; dex is the sole SSO authority. The `0.x` default has kept every non-overriding installation frozen on muster `0.24.0` since the `1.0.0` bump, so they never received the `1.x` line (including the `#37164` SSO re-exchange deauth fix first released in muster `v1.0.7`). Every fleet installation already runs dex-only (`shared-configs` sets none of the removed keys — `enableJWTMode`/`jwtSigningKey`/`localMint`/M2M), and alligator already runs `1.0.7` healthy, so the jump is safe. **Upgrade note:** crossing into muster `1.0.7+` re-authenticates every user exactly once (the mcp-oauth bump changes the Valkey token-storage layout with no legacy read-fallback); existing sessions do not carry over.
  Second crossing, muster `v2.0.0` ([muster#1064](https://github.com/giantswarm/muster/pull/1064)): the `writesAsCaller` transition gate is gone, so in Kubernetes mode session-initiated MCPServer mutations (create/update/delete and CR-driven start/stop/restart) always write with the caller's own dex id_token, authorized by k8s RBAC via the chart-shipped `mcpserver-editor` role (bound to all authenticated users by default). Sessions must carry the dex-k8s-authenticator audience and the apiserver must trust dex OIDC — already true fleet-wide. The gate lived two hours and no installation ever set it.
  Third crossing, muster `v3.0.0` ([muster#1072](https://github.com/giantswarm/muster/pull/1072), [muster#1067](https://github.com/giantswarm/muster/issues/1067)): `type: stdio` MCPServers are rejected in Kubernetes mode — a stdio server is started as a subprocess of the muster pod, so accepting one made "may write MCPServer resources" mean "may execute code in the muster pod as muster's ServiceAccount". Validate/create/update fail the tool call; a stdio CR applied with `kubectl` reconciles to `status.state: Failed` and is never started. **Nothing to migrate:** no gitops manifest in the org and no live installation declares a `type: stdio` MCPServer (all are `streamable-http`), which is also why this crossing did not need to be a major. Should one ever be needed, run the MCP server as its own workload and register it with `type: streamable-http` or `sse`; `stdio` still works when muster runs as a local CLI.
- `kagent.controller.auth.userIdClaim` default changed from `sub` to `email` (umbrella + connectivity `values.yaml`). The kagent controller now keys sessions and identity on the dex `email` claim instead of dex's opaque, connector-prefixed `sub`. This matches muster's trusted-issuer `subjectClaim: email` and makes the UI path (oauth2-proxy forwards the same dex id_token) and the A2A path resolve to the same identity. Existing kagent sessions keyed by old `sub` values are orphaned by this change (session history resets); the claim falls back to `sub` in the controller when `email` is absent from the token.

### Added

- `/connectors/complete` added to the `klausgateway-obo` HTTPRoute (connectivity chart), routing straight to the klaus-gateway Service on the OBO public hostname alongside `/auth/slack/`. This is the connector-login completion landing: after a connector sign-in muster redirects the browser here (klaus-gateway#175), so it must bypass the agentgateway JWT policy that guards the channel paths (the browser carries no JWT at that point). Only rendered when `klausGateway.obo.enabled` and `ingress.parentRefs` are set. Without it a `postLoginRedirectAllowlist` entry pointing at this path would 404 at the data plane and the flow would degrade to muster's static success page.
- `klausGateway.obo.connectors` (umbrella + connectivity `values.yaml` and `values.schema.json`) is forwarded to the klaus-gateway subchart, which enables the reactive Slack connector UX (`obo.connectors.enabled` -> `KLAUS_GATEWAY_OBO_CONNECTORS_ENABLED`): the gateway detects a `core_auth_login` challenge in the agent's response stream and offers a "Connect <backend>" button from the login link the agent relays. Disabled by default. The `obo` block in both schemas is `additionalProperties: false`, so a cluster setting this key on 2.4.2 fails the HelmRelease schema check (`Additional property connectors is not allowed`); this entry closes that gap.
- `kagent.controllerRoute.jwtAuthentication` (umbrella + connectivity `values.yaml` and `values.schema.json`): opt-in JWT validation on the inner `kagent-controller` HTTPRoute. When enabled, the connectivity chart renders an `AgentgatewayPolicy` (dex issuer, no required audiences) and a static `AgentgatewayBackend` (`kagent-controller-jwks`) for the JWKS endpoint, so agentgateway verifies the bearer's signature, issuer, and expiry against dex before forwarding to the kagent controller. `kagent.controllerRoute.jwtAuthentication.mode` selects the validation strictness (enum `Strict`, `Optional`, `Permissive`; default `Strict`): `Strict` rejects requests with a missing or invalid token, `Optional` validates a token when present but allows requests with none, `Permissive` validates without rejecting so an install can confirm real traffic carries valid dex tokens before enforcing. This makes the trusted-proxy assumption of `kagent.controller.auth.mode: trusted-proxy` hold for A2A callers routed through agentgateway; previously no policy existed and any parseable JWT passed. Intra-namespace agent pods (`app: kagent`) that call the controller directly on 8083 (allowed by the kagent NetworkPolicy) bypass agentgateway and this policy, and are out of scope. Off by default: enabling it rejects callers that do not present a dex-issued JWT (opaque muster tokens, Kubernetes ServiceAccount tokens), so enable it per install only once all A2A callers forward dex JWTs. Requires `gateway.jwksEgress.enabled: true` so the data plane can reach dex. A `ci/test-kagent-jwt-policy-values.yaml` render test covers the new resources, and a render-time guard fails when it is enabled without an issuer.
- `dicebear` avatar renderer as a force-enabled meta-package component (umbrella `values.yaml` `components.dicebear` + a forwarded top-level `dicebear:` block, `values.schema.json`, `examples/customer-bom.yaml` pinned to `0.3.0`). Wires the self-hosted [giantswarm/dicebear](https://github.com/giantswarm/dicebear) chart into the bundle so the DiceBear avatar API is deployed on **every** cluster that runs the platform, per [giantswarm/giantswarm#37211](https://github.com/giantswarm/giantswarm/issues/37211). Rendered as an OCIRepository + HelmRelease like every other component (`injectGlobal: false` — the chart's strict schema consumes `registry.domain`, not `global`). Its public avatar `HTTPRoute` is **on by default**, so every installation must set `dicebear.route.parentRefs` (typically the same public Gateway as `ingress.parentRefs`) and `dicebear.route.hostnames` (e.g. `avatars.<domain>`) — the chart hard-fails otherwise, by design, matching the existing `ingress.*` per-cluster contract.
- Shared muster `RemoteMCPServer` for tenant agents. The connectivity chart now renders a single `RemoteMCPServer` named `muster` in the release namespace (`agentic-platform`) with `allowedNamespaces: {from: All}`, so agents in any namespace can reference the muster gateway cross-namespace — this is the admin-owned server the generic [`agent` chart](https://github.com/giantswarm/agent) defaults its `serverRef` to (`muster`/`agentic-platform`), per the [creating-agents PRD](https://github.com/giantswarm/bumblebee-plans/blob/main/creating-agents/PRD.md). On-behalf-of only: the server sets no `headersFrom`, so the caller token forwarded by kagent (`KAGENT_PROPAGATE_TOKEN`) is the only `Authorization` reaching muster, and muster authenticates every call. Rendered unconditionally whenever `kagent.enabled` and `muster.enabled` — the CR is inert until an agent references it — and the name is fixed by the template: name/namespace are the platform contract the agent chart's default `serverRef` depends on, so neither is a value.
- `agents.models[].displayName` (umbrella + connectivity `values.yaml` and `values.schema.json`): an optional human-friendly name for a kagent `ModelConfig`. When set, it is rendered as the `ui.giantswarm.io/display-name` annotation on the generated `ModelConfig` CR so the UI can show a friendly label; when omitted, no `annotations:` block is emitted. This change also adds the previously-missing `agents.models` schema (required `name`/`provider`/`model`/`apiKeySecret`, optional `displayName`/`apiKeySecretKey`, `additionalProperties: false`) so model entries are now validated.
- `klausGateway.obo.persistence` (umbrella + connectivity `values.yaml` and `values.schema.json`) is forwarded to the klaus-gateway subchart, which backs the encrypted link store with a ReadWriteOnce PVC so a human's Slack<->muster link survives gateway pod recreation (rollouts, node drains), not just in-process restarts. Disabled by default (emptyDir, unchanged). Requires the klaus-gateway chart that supports it (giantswarm/klaus-gateway#118, v0.10.3). Pairs with that release's store-key fix, which accepts the SOPS-staged base64 `obo.storeKey` directly so `obo.storePath` can finally be set without the gateway crashing on boot.
- klausgateway Slack OBO (on-behalf-of) wiring. The `klausGateway.obo.*` block (`enabled`, `musterUrl`, `callbackBaseUrl`, `storePath`, `stateKey`, `storeKey`) is now declared and documented in both the umbrella and connectivity `values.yaml` (with matching `values.schema.json` entries) so it is forwarded to the klaus-gateway subchart. The connectivity chart renders a new public `HTTPRoute` (`klausgateway-obo`) that exposes the unauthenticated OAuth-bootstrap paths `/auth/slack/link`, `/auth/slack/callback`, and the CIMD document `/auth/slack/client.json` on the gateway's public hostname (derived from `obo.callbackBaseUrl`), routing straight to the klaus-gateway Service so they bypass the JWT policy that guards the channel paths. Both the linking browser and muster (CIMD fetch) reach the gateway over this route. Disabled by default. A `ci/test-klausgateway-obo-values.yaml` render test covers the new route.
- `klausGateway.slack.dmOnly` (umbrella `values.yaml` + `values.schema.json`) is forwarded to the klaus-gateway subchart, which renders it as `SLACK_DM_ONLY` to restrict the Slack adapter to direct messages (channel messages and @-mentions ignored). Recommended for the OBO gateway, which is a DM-only bot. Default false. Requires the klaus-gateway chart that renders the env (giantswarm/klaus-gateway#113).
- `klausGateway.slack.botToken` / `signingSecret` / `appToken` (umbrella `values.yaml` + `values.schema.json`) are forwarded to the klaus-gateway subchart, which renders the `slack.secretName` Secret (keys `bot-token` / `signing-secret` / `app-token`) from them. This lets the per-cluster `secret-values.yaml.patch` stage SOPS-encrypted Slack credentials that become the `slack-credentials` Secret the gateway consumes. Requires the klaus-gateway chart that renders the Secret (giantswarm/klaus-gateway#114).

### Fixed

- Removed duplicate `channelMode` / `channelAllowlist` / `dmMode` entries in the umbrella `values.schema.json` `klausGateway.slack` block (each was declared twice).
- `klausGateway.slack.channelMode` / `channelAllowlist` / `dmMode` added to the umbrella `values.schema.json`. The klaus-gateway subchart (0.20.x) introduced channel-mode routing (`channelMode`: `all`/`allowlist`/`none` with `channelAllowlist`, and `dmMode`: `serve`/`redirect`/`ignore`), but the umbrella schema's `klausGateway.slack` block (`additionalProperties: false`) was never extended, so any installation setting these keys — e.g. gazelle's swarmgeist channel pilot ([giantswarm-configs#762](https://github.com/giantswarm/giantswarm-configs/pull/762)) — failed the helm upgrade at schema validation (`Additional property channelMode is not allowed`), leaving the HelmRelease stuck in `Failed` and blocking all pending upgrades (PagerDuty incident #6917). The umbrella now mirrors the subchart's schema for these three keys.
- The same `channelMode` / `channelAllowlist` / `dmMode` keys are declared in the `agentic-platform-connectivity` `values.schema.json` too. The umbrella forwards its whole `klausGateway` block to the connectivity HelmRelease via `forwardAllValues`, so fixing only the umbrella would relocate the `Additional property channelMode is not allowed` failure to the connectivity release once the umbrella upgrade reconciles.
- `agentic-platform-connectivity`: the `kagent-srt-settings` Kyverno policy now reads the existing `srt-settings.json` from the Secret's `data` (base64-decoded) instead of `stringData`. The API server folds `stringData` into `data` before admission webhooks run (`Convert_v1_Secret_To_core_Secret`), so the policy's merge source was always empty and the mutation replaced the controller-generated settings with `{"enableWeakerNestedSandbox":true}` alone. srt then rejected the file (`network: Required, filesystem: Required`), which broke sandboxed skill execution and left agents without their muster MCP tools ("Tool not found" on `filter_tools`/`call_tool`/`describe_tool`). The merged file now keeps the controller's `network` and `filesystem` sections alongside the flag. Existing agent config Secrets are only re-mutated on their next write; restart the kagent controller after deploying to force a rewrite.
- kagent A2A routing: the inner `kagent-controller` HTTPRoute (on the agentgateway data-plane Gateway) is no longer hostname-scoped. It was rendered with `hostnames: [<public hostname>]` from `kagent.controllerRoute.hostname`, but in-cluster A2A callers — notably klaus-gateway's `--a2a-url`, which targets the agentgateway Service's cluster-DNS name (`agentgateway.<ns>.svc.cluster.local`) — send that Service host, which did not match the public hostname. The request fell through to the catch-all MCP route (`agentic-platform-mcps`, path `/`), was handled as MCP Streamable-HTTP, and was rejected with `406 Not Acceptable ("mcp: client must accept both application/json and text/event-stream")`, so the Slack OBO sre-agent turn never reached the agent (it had passed JWT validation — the human token was forwarded — but died at content negotiation on the wrong route). The inner hop is internal and must match any Host; the `/kagent` prefix is more specific than the MCP `/` route, so a hostname-less route wins for every Host. The outer `kagent-controller-public` route keeps the public `hostname` (required on the shared TLS Gateway).
- klausgateway channel routing: a new `*-dataplane-to-klausgateway` egress policy (cilium + kubernetes flavors, rendered when `klausGateway.agentgatewayRoute.enabled`) lets the agentgateway data-plane reach the `klaus-gateway` Service on `:8080`. The channel paths (`/v1`, `/web`, `/cli/v1`, and `/channels/slack` when `slack.enabled`) are served on the data-plane Gateway and forwarded to klaus-gateway, but the data-plane runs in default-deny egress (the `-dataplane` policy) whose cluster allowance only covers `80`/`443`. klaus-gateway listens on `8080`, so the forward was dropped and every channel request — including inbound Slack events delivered to the public hostname — failed with `503 UpstreamFailure ("Connect: deadline has elapsed")`, so the Slack bot never replied. Mirrors the existing `-dataplane-to-kagent` allowance.
- `agentic-platform-connectivity` `values.schema.json`: allow `klausGateway.slack.{dmOnly,botToken,signingSecret,appToken}`. The umbrella forwards its whole `klausGateway` block to the connectivity HelmRelease via `forwardAllValues`, but the connectivity `slack` schema had `additionalProperties: false` and only declared `enabled`/`mode`/`secretName`, so a real install (gazelle) failed the HelmRelease upgrade with `Additional property dmOnly/botToken/signingSecret/appToken is not allowed`. These keys are consumed by the klaus-gateway subchart, not this chart; they are now declared (and documented as forwarded-only) so validation passes.
- klausgateway Slack OBO egress: a new `klausgateway-obo-egress` NetworkPolicy (cilium + kubernetes flavors, rendered when `klausGateway.obo.enabled`) lets the klaus-gateway pod reach the muster authorization server on 443/10443 for RFC 8414 discovery and the OAuth token exchange. The gateway is put into default-deny egress by the `klausgateway-a2a-egress` policy, which only allowed DNS + the agentgateway data plane; without this allowance the OBO token call to muster's public issuer host (which resolves to the public NLB / private LB VIP) was dropped. Mirrors the existing kagent-agent and oauth2-proxy `world`+`cluster` 443/10443 egress.
- klausgateway connectivity route: the `AgentgatewayBackend` `.spec.static.host` now defaults to the correct `klaus-gateway` Service name (the klaus-gateway chart's default, matching `templates/klausgateway/netpol.yaml`) instead of `klausgateway`, which resolved to a non-existent Service when `klausGateway.fullnameOverride` was unset.

### Removed

- **Agent creation and the whole `agents.*` values key.** Agents are no longer created by this chart — install one release of the generic [`agent` chart](https://github.com/giantswarm/agent) per agent instead, per the [creating-agents PRD](https://github.com/giantswarm/bumblebee-plans/blob/main/creating-agents/PRD.md). The `agents.definitions` block is removed from the umbrella and connectivity `values.yaml`, along with the templates it drove in `agentic-platform-connectivity`: the kagent `Agent` CR + chart-owned `ServiceAccount` (`agents.yaml`), the per-agent `muster-<name>` `RemoteMCPServer` loop (the `agent` chart references the shared `muster` server cross-namespace instead), the per-agent A2A `AgentgatewayBackend` + `/agents/<name>` `HTTPRoute` (`agentgateway-backends.yaml` — not part of the PRD's chart scope and unused: in-cluster A2A callers go through the kagent controller route; use `extraObjects` for one-offs), and the now-unused `_agents.tpl` helpers. The two admin-owned lists that remained (`agents.models`, `agents.remoteMcpServers`) moved into the `kagent` block — see Changed — dissolving the `agents` key entirely. Leftover `agents.*` keys in per-cluster values are silently ignored (the root schema is deliberately permissive for `forwardAllValues`), which enables a zero-gap migration on the auto-updating dev track: add the `kagent.*` keys alongside the old ones, let the chart roll out, then delete the old keys. The rest of the admin-owned surface the `agent` chart depends on stays: the shared `muster` `RemoteMCPServer` and the kagent-namespace policies (muster-egress CNP, Kyverno exception, declarative-agent pod security), which apply to agent pods regardless of which chart created them. **Migration:** any cluster with `agents.definitions.sre-agent.enabled: true` must have an `agent`-chart-based replacement (carrying the eval-tuned `systemMessage`) deployed *before* upgrading, or the live Agent CR is deleted; per-cluster values setting `agents.models`/`agents.remoteMcpServers` should add the equivalent `kagent.modelConfigs`/`kagent.remoteMcpServers` keys **before** the chart rolls out (old keys can be removed afterwards).
- **The per-agent muster token machinery and the `agents.definitions.<agent>.obo` flag.** The `muster-token-init` Job, the `muster-refresh` CronJob, the shared token pod-spec helper, the Secret-writer Role/RoleBinding, the static `headersFrom` `Authorization` on the per-agent muster `RemoteMCPServer`, and the `agents.muster.{kubectlImage,busyboxImage,tokenExpirationSeconds,tokenRefreshSchedule}` values are removed from `agentic-platform-connectivity` (and the umbrella defaults). With M2M gone, acting on behalf of the user is the only mode: the RemoteMCPServer carries no static credentials and kagent's propagated user token (`KAGENT_PROPAGATE_TOKEN`) is the sole `Authorization` reaching muster, so the `obo` toggle has nothing left to switch. `agents.remoteMcpServers[].tokenSecret` now only accepts a pre-existing Secret; the chart no longer writes one.
- **M2M agent impersonation RBAC.** The `agents.<name>.m2m` block (and the `impersonation.yaml` / `_agent-impersonation.tpl` templates it rendered) is removed from `agentic-platform-connectivity`. Agents no longer get a deputy `Role`/`ClusterRole` letting mcp-kubernetes impersonate the agent's own identity or bind its groups to ClusterRoles. The only supported flow is on-behalf-of: muster mints a token with the human as `sub` and the agent SA in the RFC 8693 `act` claim, and the mcp-kubernetes chart's `*-obo-impersonate` ClusterRole authorizes impersonating the human. Requires mcp-kubernetes with M2M removed and muster with the matching broker config.
- **Retired the `agentic-platform-crds` bundle chart** — every component now owns its CRDs (app-owned CRDs). The standalone chart (`helm/agentic-platform-crds/`), its CircleCI build/test/push jobs, and the now-dead Renovate `helmv3` lockstep `packageRules` are deleted. There is no longer a `components.agentic-platform-crds` entry in the meta-package.
- `klausGateway.slack.dmOnly` (umbrella `values.yaml` and both `values.schema.json`). klaus-gateway removed the `SLACK_DM_ONLY` env and the `slack.dmOnly` value, replacing them with `dmMode` + `channelMode`, and already ignores a leftover `dmOnly`. The umbrella drops the dead default and, with `additionalProperties: false`, no longer accepts the key. **Migration:** `dmOnly: true` becomes `dmMode: serve` + `channelMode: none`; the old `dmOnly: false` (channels served, DMs redirected) becomes `dmMode: redirect` + `channelMode: all`. An install still setting `dmOnly` (e.g. graveler [giantswarm-configs#742](https://github.com/giantswarm/giantswarm-configs/pull/742)) must migrate before this merges.

### Changed

- **`agents.models` → `kagent.modelConfigs`, `agents.remoteMcpServers` → `kagent.remoteMcpServers`** (umbrella + connectivity `values.yaml` and `values.schema.json`). With agent creation gone (see Removed), the leftover `agents` key only held admin-owned kagent CRs, which was misleading; both lists now live in the `kagent` block next to `providers` (whose `anthropic` entry already yields the subchart-created `default-model-config`), under the same "read by connectivity templates, not the subchart" convention as `controllerRoute`/`uiRoute`. `models` is renamed to `modelConfigs` to match the CR kind. The rendering templates moved up from `templates/kagent/agents/` to `templates/kagent/`, and `kagent.remoteMcpServers` gains the schema validation it never had (required `name`/`url`, optional `description`/`tokenSecret`, `additionalProperties: false`). Note these keys ride into the kagent component's HelmRelease values (ignored by that chart today, like the routing keys) — if upstream kagent ever introduces same-named values, they must be renamed here.
- `kagent.oauth2-proxy`: request the `offline_access` scope and set `cookie-refresh: "10m"` so oauth2-proxy refreshes the upstream Dex id_token before it expires. Without this, a session older than the Dex id_token TTL forwarded an expired bearer as the on-behalf-of subject token, muster rejected it (`subject_token_validation_failed`, "token is expired"), and the agent silently fell back to its ServiceAccount (M2M) identity. Applied to both the umbrella `agentic-platform` and the `agentic-platform-connectivity` `values.yaml` (kept in sync; the umbrella copy is the one deployed in meta-package installs).
- `agents.definitions.sre-agent`: applied the eval-tuned muster `systemMessage` to the **umbrella** `agentic-platform` chart's `values.yaml` as well. The umbrella forwards its own values to the `agentic-platform-connectivity` HelmRelease via `forwardAllValues`, so in meta-package installs (every real deployment, e.g. glean) the umbrella's copy shadows the connectivity sub-chart default — without this the previous change had no effect on deployed clusters. The two `values.yaml` files now carry a reciprocal keep-in-sync note.
- `agents.definitions.sre-agent`: list muster's meta-tools explicitly under `toolNames` (was empty = all) so the kagent Agent Details UI renders them by name instead of a single "Unknown Tool" entry. The agent's available tools are unchanged — backend tools (`x_kubernetes_*`, `x_prometheus_*`, workflows) are still reached through `call_tool`. Applied to both the umbrella `agentic-platform` and `agentic-platform-connectivity` `values.yaml` (kept in sync).
- `agents.definitions.sre-agent`: the default `systemMessage` now carries the muster meta-tool contract that was eval-tuned for the Backstage devportal AI chat ([giantswarm/backstage#1775](https://github.com/giantswarm/backstage/issues/1775)) — prefer a single-call `workflow_<name>` (discovered via `filter_tools(query=…)`), the exact `x_kubernetes_*` argument contract (`management_cluster: <mc>-mcp-kubernetes`, `resourceType`, `podName`, `tailLines`), and the cheap-listing recipe (`summary`, `fieldSelector: status.phase!=Running`, `reason=BackOff` for CrashLoopBackOff). The agent shares muster's meta-tool interface with the devportal chat, so the same guidance reduces wrong-arg retries and avoidable tool round trips. Clusters that enable the agent without overriding `systemMessage` (e.g. glean) inherit it.
- `agentic-platform-connectivity`: the agentgateway data-plane container now sets `ephemeral-storage` requests/limits (new `gateway.parameters.dataPlaneResources` value, merged onto the generated proxy container via `AgentgatewayParameters`). The agentgateway controller injects a writable `/tmp` emptyDir (readOnlyRootFilesystem) without a `sizeLimit`, which tripped the `require-emptydir-requests-and-limits` Kyverno policy; declaring ephemeral-storage on the mounting container clears the audit warning. Part of the namespace-wide Kyverno cleanup (giantswarm/giantswarm#36885).
- The bundled `valkey` (`muster-valkey`) component now documents that its RDB-only persistence is a deliberate cache-only choice and warns against enabling AOF to "fix" data loss: AOF lives on the same PVC so it does not survive PVC loss/recreation (the failure mode that actually occurs), and flipping `appendonly` in config + restart is a data-loss footgun (Valkey loads the empty AOF dir and ignores the RDB). Comment-only; no behaviour change. See [giantswarm/muster#884](https://github.com/giantswarm/muster/issues/884).
- `agentic-platform-connectivity`: the kagent controller `NetworkPolicy` (both `cilium` and `kubernetes` flavors) scopes intra-namespace ingress to agent pods (`app: kagent`) and the kagent UI on the controller API port (8083), instead of allowing every pod in the kagent namespace on all ports.
- `agentic-platform-connectivity`: kagent agent pods may egress to the cluster Envoy LB VIP on 443/10443, so the STS plugin can reach muster's OAuth token endpoint (served on the public ingress hostname, which resolves to a private cluster VIP) for on-behalf-of token exchange.
- **App-owned CRDs, release B + bundle retirement** (completes the staged migration begun in release A). `agentgateway`, `kagent`, and `agent-sandbox` drop their `dependsOn: [agentic-platform-crds]` (they already ship their CRDs with `crds: CreateReplace`). The CR consumers repoint to the CRD-owning components: `agentic-platform-mcps` now `dependsOn: [muster, agentgateway]` and `agentic-platform-connectivity` now `dependsOn: [agentgateway, kagent]` (both were `agentic-platform-crds`). The handoff is non-destructive: release A had already overwritten the live agentgateway/kagent/kmcp CRDs with the wrapper charts' `helm.sh/resource-policy: keep` copies, so Flux pruning the retired bundle `HelmRelease` cannot delete the CRDs or cascade to any CR.
- The meta-package's generic component loop now **drops a `dependsOn` reference to a component that is toggled off** (new `agentic-platform.componentEnabled` helper). With app-owned CRDs a CR consumer dependsOn the opt-in component that ships the CRD it needs; in the default `muster-direct` topology `agentgateway`/`kagent` are off and render no `HelmRelease`, so without this filtering the always-on `agentic-platform-connectivity` release would block forever on a dependency that was never rendered.

### Added

- `agents.sreAgent`: opt-in bundled Declarative kagent `Agent` (`sre-agent`) that reaches muster machine-to-machine through the static `muster` RemoteMCPServer (SA Bearer token from the `<name>-kagent-muster-token` Secret). `spec.declarative.deployment.serviceAccountName` is omitted so the kagent controller auto-creates the `sre-agent` SA. Configurable `modelConfig` (default `default-model-config`), `systemMessage`, `toolServer` (default `muster`), `toolNames`, `allowedHeaders`, `deployment.resources`, and `deployment.env`. `allowedHeaders` is inert: forwarding a request header onto muster tool calls additionally needs kagent-dev/kagent#2044, absent from the pinned kagent 0.9.9. Disabled by default.
- `agents.sreAgent.m2m`: opt-in impersonation RBAC for the agent's M2M identity. `m2m.impersonators` (list of `{name, namespace}` SAs, default `mcp-kubernetes`) get a `Role`/`ClusterRole` allowing them to impersonate `m2m.granted`. A `system:serviceaccount:…` `granted.user` renders a namespaced `serviceaccounts` Role + groups `ClusterRole`; a plain username renders a single `users`+`groups` ClusterRole. `m2m.granted.clusterRoles` (default `[]`) binds the granted groups to the listed ClusterRoles (the agent's K8s authz, e.g. `read-all`), one ClusterRoleBinding each. The matching muster `workloadGroupGrant` and mcp-kubernetes `impersonateUser`/`impersonateGroups` are configured per cluster and must agree with `m2m.granted`. Disabled by default.

### Removed

- `agents.muster.userServer` and the header-less `muster-user` RemoteMCPServer. On the pinned kagent 0.9.9 it lists zero tools (no static `Authorization` for controller discovery) and the forwarded-token override it depended on (kagent-dev/kagent#2044) is absent, so it could not impersonate the end user. User impersonation returns once kagent ships #2044 and muster carries the human identity (`email`/`groups`).

### Changed

- muster CRDs (`MCPServer` / `Workflow`) now follow the **app-owned CRDs** pattern instead of riding the `agentic-platform-crds` bundle. The meta-package's generic component loop gains an optional `crds` policy field (rendered into the child `HelmRelease`'s `spec.install.crds` / `spec.upgrade.crds`); the `muster` component sets `crds: CreateReplace` and no longer `dependsOn` the bundle, so its CRDs travel with the muster app chart at the same resolved version and upgrade atomically on every release (Flux `CreateReplace` overcomes Helm's "never upgrades `crds/`-dir CRDs" limitation). `agentic-platform-mcps` now `dependsOn: [muster, agentic-platform-crds]` (its `MCPServer` CRs need the muster-owned CRD; its agentgateway CRs still need the bundle). The `agentic-platform-crds` bundle drops its `muster-crds` dependency (it still ships agentgateway / kagent / agent-sandbox CRDs). The handoff is non-destructive: the live muster CRDs carry `helm.sh/resource-policy: keep`, so dropping the bundle dependency does not delete them — the muster release adopts and upgrades them on its next reconcile. Requires a muster app chart that ships its CRDs in `crds/` (tracked separately).

- **App-owned CRDs, release A** for the three remaining bundle components. `agentgateway` and `kagent` are repointed from their upstream chart repositories to the Giant Swarm wrapper charts (`oci://gsoci.azurecr.io/charts/giantswarm/{agentgateway,kagent}`, from `giantswarm/agentgateway` and `giantswarm/kagent-app`) that vendor the upstream chart as a subchart and ship the `agentgateway.dev` / `kagent.dev` + `kmcp` CRDs in their own `crds/` dir with `helm.sh/resource-policy: keep`. `agent-sandbox` already pointed at its GS chart. All three now set `crds: CreateReplace`. This is the **non-destructive first step** of the staged handoff: every component KEEPS its `dependsOn: [agentic-platform-crds]` and the bundle keeps shipping the CRDs, so `CreateReplace` overwrites the live (un-annotated) CRDs with the `keep`-annotated copies before any bundle dependency is dropped (release B). The meta-package's generic component loop gains an optional `valuesKey` field: the `agentgateway` / `kagent` blocks are nested under the subchart key when forwarded to the wrapper chart, leaving the flat top-level blocks (and every per-cluster override path and the connectivity chart's reads of them) unchanged. Wrapping the upstream kagent chart as a subchart keeps its `.Chart.Version` a clean `0.9.9`, so the `app.kubernetes.io/version` label is no longer corrupted by the OCI `+digest` — the `kagent` version pin (`0.9.9` -> `0.x`) and the `postRenderers` kustomize label-sanitization hack are both removed.

- Bump `muster` sub-chart `0.8.4` -> `0.9.0`, which adds a cheap, ranked, faceted tool-discovery tier (giantswarm/muster#868): `filter_tools` is now a discovery tier distinct from execution, so finding a tool no longer scales with the full descriptive weight of every candidate. It gains `limit`/`offset` (default page 25) returning `total` + `truncated`, defaults to a one-line `summary` per tool with no input schema (full description/schema stay available via `describe_tool` or `include_schema=true`), adds a BM25-ranked `query` mode that returns matches best-first with a `score`, and exposes `Workflow` CRD `metadata.labels` as a filterable `labels` facet. Against a ~280-workflow fleet a broad `filter_tools(pattern="*workflow*")` call drops from a ~330 KB full-catalogue dump to a ~3 KB summary page (~100x smaller). The companion `muster-crds` / `agentic-platform-crds` chart is intentionally NOT bumped: #868 propagates existing Kubernetes labels onto tool metadata and changes no CRD schema, so muster 0.9.0 runs against the existing 0.8.4 CRDs.

- Bump `muster` (and `muster-crds`) sub-chart `0.7.5` -> `0.8.4`, which adds workflow control flow (giantswarm/muster#865): `forEach` (sequential loops over a list), `parallel` (concurrent sub-step groups with isolated contexts), workflow-level `onFailure` (best-effort cleanup/rollback), and `condition.template` (boolean Go-template step gates). The `muster-crds` chart carries the matching `Workflow` CRD schema (new `spec.steps[].forEach`/`parallel`, `spec.onFailure`, `condition.template`; the dead `step.outputs` field is removed in favour of `store: true` + `{{ .results.<step_id> }}`), so the CRDs chart must be upgraded alongside the app chart. Also fixes camelCase field-name parsing (`allowFailure`/`fromStep`/`expectNot`/`jsonPath`) in the structured `workflow_create`/`update`/`validate` tool path. The `0.8.4` bump (over `0.8.0`) carries giantswarm/muster#871: the `0.8.0` `Workflow` CRD was rejected by the Kubernetes API server on k8s >= 1.34 because its "exactly one of" CEL guards used a `[...].filter(x, x).size() == 1` form whose estimated cost exceeded the schema-wide validation budget (3.2x over), which made `agentic-platform-crds` fail to install/upgrade and roll back. Rewriting the guards to the cheap additive form fixes the install.

- klausgateway: the agentgateway HTTPRoute now includes `/channels/slack` when `klausGateway.slack.enabled` is true, exposing the Slack Events API webhook endpoint through the data-plane.

### Fixed

- agent-sandbox: the controller Deployment install no longer loops on Giant Swarm
  clusters that enforce restricted-PSS via Kyverno (fail-closed). The Kyverno mutate
  policy that injects the controller's `securityContext` ships in the same Helm
  release as the Deployment, so Kyverno could not load it before the Deployment was
  admitted — admission was denied, the release rolled back (deleting the policy too),
  and Flux retried indefinitely. The policy now carries `helm.sh/resource-policy: keep`
  so it survives the first-install rollback; Kyverno then loads it and the next upgrade
  retry re-applies the (now-mutated) Deployment, which passes admission.

### Changed

- Bump `muster` sub-chart `0.5.6` -> `0.5.7`, which bumps `mcp-oauth` to `v0.4.2`, making the token-exchange broker's trusted-issuer JWKS cache rotation-safe: it now refetches the issuer JWKS when it encounters an unknown `kid` (giantswarm/muster#847). Without this, a routine Dex signing-key rotation left the broker serving a pre-rotation JWKS and rejecting **every** current user token with `subject_token_validation_failed` until muster was restarted — a fleet-wide devportal logout.

- Bump `muster` sub-chart `0.5.5` -> `0.5.6`, which bumps `mcp-oauth` to `v0.4.1`. The token-exchange broker and MCP OAuth client now RFC 6749-encode client credentials before HTTP Basic auth, so token-exchange client secrets containing `+` (and other reserved characters) no longer fail Dex client authentication with `invalid_client`. Unblocks the tunnelport private-cluster devportal rollout (giantswarm#36880) for clusters whose `muster-token-exchange-<mc>` secret contains `+` without per-cluster secret rotation.

### Fixed

- `agentic-platform-kagent-agent-muster-egress` CNP was missing an egress rule for kagent agent pods to reach the kagent-controller on port 8083. Agent pods had no path to dial into the controller, blocking agent-to-controller communication.

- muster token hook Job (`kagent-muster-token-init`): no longer depends on a shell in the `kubectl` image, which is distroless (`registry.k8s.io/kubectl`) and crash-looped `BackoffLimitExceeded` on `/bin/sh: no such file or directory`, failing post-upgrade. The token is now minted via a projected `serviceAccountToken` volume, the `Bearer <token>` Secret manifest is rendered by a busybox init container, and `kubectl` is invoked with args only to apply it. The Job runs as `kagent-muster-client` (the identity muster trusts); the `serviceaccounts/token` create RBAC is dropped.

### Changed

- `agents.muster`: replaced `tokenDuration` (Go duration) with `tokenExpirationSeconds` (integer, fed to the projected token volume); added `busyboxImage`.

- `klausGateway.a2a.url` now routes through the agentgateway data-plane Service (`http://agentgateway.agentic-platform.svc.cluster.local:8080/kagent/api/a2a/kagent`) instead of hitting `kagent-controller:8083` directly, so A2A egress is authenticated and observed by agentgateway. Requires a klaus-gateway release that forwards the caller's bearer token.

### Added

- `templates/klausgateway/netpol.yaml`: NetworkPolicy (cilium + kubernetes flavors) allowing klaus-gateway egress to the agentgateway data-plane Gateway on port 8080, rendered when `klausGateway.enabled` and `klausGateway.a2a.enabled`.

- Bundle the [agent-sandbox](https://github.com/kubernetes-sigs/agent-sandbox) controller
  (opt-in via `agentSandbox.enabled`, default off) and its CRDs (Sandbox / SandboxTemplate /
  SandboxClaim / SandboxWarmPool, shipped via `agentic-platform-crds`, keep-protected). This
  is the Sandbox runtime kagent's `SandboxAgent` delegates pod isolation to — `agentic-platform-crds`
  already shipped the `SandboxAgent` CRD, but nothing installed the controller it requires.
  Restricted-PSS securityContext is injected into the controller Deployment via an umbrella
  Kyverno mutate policy, since the vendored upstream chart exposes no securityContext knob.
  A render-time guard (`templates/agent-sandbox/validate.yaml`) fails the install early if
  `agentSandbox.podSecurity.namespace` drifts from the controller's actual namespace, which
  would otherwise leave the Kyverno policy silently targeting the wrong namespace.

### Security

- The kagent muster M2M token now defaults to a 1-hour TTL (`agents.muster.tokenExpirationSeconds: 3600`, was 365 days) and is re-minted by a new `muster-token-refresh` CronJob on `agents.muster.tokenRefreshSchedule` (default every 15 minutes). The bootstrap Job still seeds the Secret on install/upgrade; kagent's RemoteMCPServer picks up the rotated Secret without restarting the agent. Removes the year-long at-rest bearer token.

## [1.1.33] - 2026-06-16

### Changed

- Bump `agentic-platform-mcps` sub-chart `0.3.0` -> `0.4.0`, which adds
  `identityProviders.<name>.expectedIssuer` (rendered into the muster MCPServer
  `tokenExchange.expectedIssuer`). Required to repoint tunneled MCP servers at
  tunnelport `:8443` in-cluster Services, where the exchanged token's `iss` stays
  the public Dex issuer and must be pinned (giantswarm#36883).

## [1.1.32] - 2026-06-16

### Fixed

- muster token hook Job (`kagent-muster-token-init`): no longer depends on a shell in the `kubectl` image, which is distroless (`registry.k8s.io/kubectl`) and crash-looped `BackoffLimitExceeded` on `/bin/sh: no such file or directory`, failing post-upgrade. The token is now minted via a projected `serviceAccountToken` volume, the `Bearer <token>` Secret manifest is rendered by a busybox init container, and `kubectl` is invoked with args only to apply it. The Job runs as `kagent-muster-client` (the identity muster trusts); the `serviceaccounts/token` create RBAC is dropped.

### Changed

- `agents.muster`: replaced `tokenDuration` (Go duration) with `tokenExpirationSeconds` (integer, fed to the projected token volume); added `busyboxImage`.

## [1.1.31] - 2026-06-16

### Fixed

- `networkPolicy.musterInClusterMcpPorts`: add `8443` to the default so muster's supplementary
  egress CNP permits reaching tunnelport RemoteApp tunnels (ghostunnel terminates TLS on `8443`).
  Without it, muster's connection to in-cluster tunnel backends
  (e.g. `mcp-kubernetes-garm.agentic-platform.svc:8443`) was silently dropped by Cilium and timed
  out (`context deadline exceeded`).

## [1.1.30] - 2026-06-16

### Changed

- Bump `klaus-gateway` subchart to `0.1.5`.

## [1.1.29] - 2026-06-15

### Fixed

- `klausGateway.a2a.saToken` and `klausGateway.upstream`: set `additionalProperties: true` in the
  values schema so the klaus-gateway subchart's own pass-through keys
  (`a2a.saToken.{expirationSeconds,mountPath}`, `upstream.url`) validate. These nested objects were
  still `additionalProperties: false` after the `1.1.27` `a2a` fix, leaving the `agentic-platform`
  HelmRelease stuck `UpgradeFailed` on management clusters whose rendered config supplies them.

## [1.1.28] - 2026-06-15

### Changed

- Bump `klaus-gateway` subchart from `0.1.1` to `0.1.2`.

## [1.1.27] - 2026-06-15

### Fixed

- `klausGateway.a2a`: set `additionalProperties: true` in the values schema so the klaus-gateway
  subchart's own a2a keys (`url`, `saToken`, …) pass through validation. This was missed when
  `klausGateway.routing`/`lifecycle` were widened, leaving the `agentic-platform` HelmRelease stuck
  `UpgradeFailed` on management clusters whose rendered config supplies `klausGateway.a2a.{url,saToken}`.
- `kagent.controllerRoute`: add outer public HTTPRoute (`kagent-controller-public`) on the Envoy
  Gateway so the kagent A2A endpoint is reachable at `https://<hostname>/kagent/...`. The inner
  `kagent-controller` route (agentgateway data plane) was already present, but the missing outer
  route caused Envoy to return 404 for all requests to the hostname before they reached the
  agentgateway pod. Configurable via `kagent.controllerRoute.parentRef` (defaults to
  `giantswarm-default` / `envoy-gateway-system`).
- `AgentgatewayBackend/kagent`: add `spec.policies.auth.passthrough: {}` so the validated muster JWT
  is forwarded to kagent-controller. The agentgateway JWT filter strips the Authorization header
  after validation; without passthrough the controller's `AUTH_MODE=trusted-proxy` receives no
  header and returns 401 for every authenticated request.
- `helm.sh/chart` label: truncating long dev-build versions at 63 characters could leave a trailing `.`, producing an invalid label value and failing the Helm install/upgrade. The `chart` helper now trims trailing `.` as well as `-`.
- `templates/kagent/agents/remotemcpservers.yaml`: `headersFrom[].valueFrom` used a `secretKeyRef` block, but the kagent v1alpha2 `ValueSource` schema is flat (`type`/`name`/`key` with `type: Secret`). The CRs failed admission with `valueFrom.key: Required value`.
- `templates/kagent/agents/muster-token-job.yaml`: mint the muster token via the TokenRequest API (`kubectl create token`, duration `agents.muster.tokenDuration`, default 8760h) instead of a legacy `kubernetes.io/service-account-token` Secret. Legacy Secret tokens carry `iss: kubernetes/serviceaccount` and no `exp`, so muster's `trustedIssuers` JWT validation (cluster OIDC issuer + required expiry) can never accept them; every RemoteMCPServer call failed with `Unauthorized`. The legacy token Secret is removed; the token now rotates on every install/upgrade.
- `templates/kagent/agents/muster-sa.yaml`: the token-init Role granted `create` on Secrets restricted by `resourceNames`, which Kubernetes RBAC never matches for create requests (the object name is not part of the authorization attributes). The hook Job failed with `secrets is forbidden` on first install. `create` is now an unrestricted (namespace-scoped) rule; `get`/`patch` stay name-restricted.
- `templates/kagent/declarative-agent-pod-security.yaml`: remove `seccompProfile: RuntimeDefault` from the injected pod and container security contexts. `RuntimeDefault` blocks `clone(CLONE_NEWUSER)` which bwrap (used by srt internally) requires for user-namespace isolation, causing every shell/bash tool invocation to fail with `bwrap: No permissions to create a new namespace`.
- `templates/kagent/policy-exception.yaml` (new): `PolicyException` in the `policy-exceptions` namespace exempting pods and deployments labelled `app: kagent` in the kagent namespace from the cluster-wide `restrict-seccomp-strict` Kyverno policy. Required because the cluster enforces seccomp profiles and we cannot simply omit `seccompProfile` without an exception.
- `templates/kagent/declarative-agent-srt-settings.yaml` (new): Kyverno policy that patches the `srt-settings.json` key in agent config Secrets (labelled `app: kagent`) on admission to include `"enableWeakerNestedSandbox": true`. Without this flag, bwrap attempts to bind-mount `/proc` into its new PID namespace, which fails inside unprivileged containers with `bwrap: Can't mount proc on /newroot/proc: Operation not permitted`. The flag is the documented srt workaround for container environments (`sandbox/linux-sandbox-utils.ts`); the kagent controller does not expose it through any CR field.
- `templates/kagent/declarative-agent-pod-security.yaml`: extend the security context mutation to `SandboxAgent` CRs. `SandboxAgent` is a distinct kind from `Agent` and was not covered by the existing rule; pods created via the `agents.x-k8s.io` Sandbox backend were blocked by `disallow-capabilities-strict`, `disallow-privilege-escalation`, and `require-run-as-nonroot` Kyverno policies.

## [1.1.26] - 2026-06-12

### Fixed

- Update muster to 0.4.1 (mcp-oauth v0.3.1): forwarded ID tokens (`trustedAudiences`) are no longer hard-rejected when the same issuer is also configured in `trustedIssuers` for the token-exchange broker — fixes Backstage AI-chat SSO forwarding returning 401 behind the agentgateway.

## [1.1.25] - 2026-06-11

### Fixed

- `gateway.jwksEgress` now also opens egress on the agentgateway **controller** network policy
  (Cilium and kubernetes flavors). The controller fetches remote JWKS centrally and distributes
  keys to the data plane via xDS, so the data-plane-only rule left JWKS fetches timing out
  (e.g. Dex on giantswarm/dex:5556 for extra JWT providers).

## [1.1.24] - 2026-06-11

### Changed

- Update agentic-platform-mcps to v0.3.0: new `agentgateway.jwt.extraProviders` value lets the
  inbound agentgateway JWT policy accept tokens from additional issuers (e.g. Dex-issued ID tokens
  forwarded by Backstage AI chat alongside muster-issued JWTs, giantswarm#36840). Also fixes the
  `identityProviders` values schema that rejected every populated provider map.

### Fixed

- `templates/kagent/declarative-agent-pod-security.yaml`: retarget the Kyverno mutate from `Deployment` (controller output) to `Agent` CR (controller input). The previous policy patched the Deployment after the kagent controller had already stamped `privileged: true` on the git-skills path, causing the API server to reject the Deployment as self-contradictory (`privileged: true` + `allowPrivilegeEscalation: false`). Mutating the Agent CR instead sets `allowPrivilegeEscalation: false` on the controller input, which trips the controller's own guard and prevents `privileged: true` from being set in the first place. A `(type): "Declarative"` condition anchor scopes the mutation to Declarative agents only.

## [1.1.23] - 2026-06-11

### Changed

- Update muster and muster-crds to v0.4.0 (via v0.3.14): muster now supports brokered RFC 8693 token exchange (giantswarm/muster#831) — external confidential clients can exchange a trusted-issuer subject token plus an `audience` parameter at `/oauth/token` for a token minted by the audience's downstream Dex. New `muster.oauth.server.tokenExchangeBroker` values block (per-client audience allowlist, audience → downstream Dex target mapping); mcp-oauth bumped to v0.3.0. Inert by default.

## [1.1.22] - 2026-06-11

### Changed

- Update muster and muster-crds to v0.3.13. Muster's OAuth server (mcp-oauth v0.2.199) now defaults the JWT access token `aud` claim to its resource identifier when a client omits the RFC 8707 `resource` parameter, so tokens from generic OAuth clients (e.g. Backstage auth providers) pass the agentgateway's strict JWT audience validation on `/mcp` instead of failing with `401 InvalidAudience`.

## [1.1.21] - 2026-06-10

### Fixed

- Downgrade bundled agentgateway and agentgateway-crds charts from `v2.2.1` back to `v1.2.1`. The upstream project released `v2.2.x` before resetting semver to `v1.0.0`; `v2.2.1` is older than `v1.0.0` and was incorrectly treated as an upgrade (agentgateway/agentgateway#1249). Block the bogus `v2.2.x` range in `renovate.json5`.

- `templates/agentgateway/agentgatewayparameters.yaml`: data-plane image override moved from the deployment container spec to `spec.image.registry/repository/tag`. Drops `AGW_XDS_SERVICE_NAME` from `controller.extraEnv` and removes the explicit controller image tag pin: the v1.2.1 chart already sets `AGW_XDS_SERVICE_NAME: agentgateway-controller` correctly via `fullnameOverride`, and the controller image tag defaults to the chart's `appVersion` (`v1.2.1`) when unset.

- `templates/kagent/ui-httproute.yaml`: oauth2-proxy backend name now resolves `oauth2-proxy.fullnameOverride` from values (falling back to `<release>-oauth2-proxy`), so the `HTTPRoute` points at the correct service when `fullnameOverride: kagent-oauth2-proxy` is set.

- Bundled kagent declarative agents (`cilium-policy-agent`, `promql-agent`, and all others) now deploy into the `kagent` namespace. The kagent `kagent.namespace` helper resolves `namespaceOverride` from the subchart's own `.Values` scope; the parent chart's `kagent.namespaceOverride: kagent` is not visible there, so agents were landing in the Helm release namespace (`agentic-platform`). Both `default-model-config` (ModelConfig) and `kagent-tool-server` (RemoteMCPServer) live in `kagent` and are resolved by bare name within the agent's own namespace, causing every agent to show `Accepted=False`. Each bundled agent subchart block now sets `namespaceOverride: kagent` explicitly.

- `templates/kagent/ui-backendtrafficpolicy.yaml`: route-level `BackendTrafficPolicy` for the kagent UI `HTTPRoute`. The cluster-wide `gateway-giantswarm-default-error-pages` policy replaces all 4xx/5xx bodies with static HTML; without a route-level override, Envoy replaces oauth2-proxy's 403 sign-in page body (which meta-refreshes to `/login`) with that static page, breaking the login flow entirely. Any route-level `BackendTrafficPolicy` overrides the gateway-wide one, so this policy is rendered with `enabled: true` by default whenever `kagent.uiRoute.enabled: true`.

### Added

- `klausGateway.enabled` (default `false`) adds `giantswarm/klaus-gateway` as an opt-in conditional dependency (`condition: klausGateway.enabled`, alias `klausGateway`). With the flag unset or false the rendered output is byte-identical to the previous chart version. When enabled, the sub-chart installs the Klaus Gateway Deployment, Service, RBAC, and ChannelRoute CRD (via `crd.install: true`). The sub-chart's own agentgateway dependency is disabled (`klausGateway.agentgateway.enabled: false`) so the umbrella's bundled agentgateway is reused.
- Single `ingress.mode` topology selector (`muster-direct` | `agentgateway-muster` | `agentgateway-direct`) that declares the whole request topology in one place. The umbrella now owns **both** public routes — muster's `/` catch-all (new `templates/ingress/muster-httproute.yaml`, rendered in all modes) and the agentgateway `/mcp` interception route — fed from a single shared `ingress.parentRefs` / `ingress.hostnames`, so the two routes can no longer drift. A template-time guard (`templates/validate.yaml`) fails fast on an invalid mode, on `ingress.parentRefs` empty in **any** mode (the umbrella-owned muster `/` route attaches to it — an empty `parentRefs` would otherwise render a route bound to no Gateway), and on `agentgateway.enabled` / `agentic-platform-mcps.agentgateway.viaMuster` disagreeing with the mode.
- `agentgateway.enabled` (default `false`) gates the agentgateway controller dependency via `condition: agentgateway.enabled` in `Chart.yaml`. In the default `muster-direct` mode the controller, its `GatewayClass`, the data-plane `Gateway`/`AgentgatewayParameters`, and the data-plane NetworkPolicies are **not installed**.
- `agentgateway-direct` mode is modelled but **fail-guarded** — install is blocked with a clear message until a DCR-capable IdP (RFC 7591/8707) lands.
- `make verify-modes` target (wired into a new CircleCI branch test job) asserts the fail-guards fire; `ci/test-full-stack-values.yaml` now exercises the previously-untested `agentgateway-muster` path.
- Route-scoped `BackendTrafficPolicy` for muster's `/` route (new `templates/ingress/muster-backendtrafficpolicy.yaml`), rendered in **all** modes when `ingress.backendTrafficPolicy.enabled` is set — not just the agentgateway `/mcp` route. This preserves muster's `401 … WWW-Authenticate` challenge against the cluster-wide error-pages policy in `muster-direct` mode (where muster serves `/mcp` directly) and restores the pre-refactor `muster.gatewayAPI.backendTrafficPolicy` behavior on muster's own route.
- Per-route `ingress.httpRoute.muster.{annotations,labels}` and `ingress.httpRoute.mcp.{annotations,labels}` overrides, merged on top of the shared `ingress.httpRoute.{annotations,labels}` (per-route keys win on collision). Lets a downstream diverge one route — e.g. a different cert-manager issuer or Envoy route policy per route — without forking the shared block.
- `kagent-crds` (`v0.9.5`, `oci://ghcr.io/kagent-dev/kagent/helm`) bundled as a sub-chart. Installs the kagent CRDs (`Agent`, `AgentHarness`, `ModelConfig`, `MCPServer`, `RemoteMCPServer`, `Memory`, `ToolServer`, `SandboxAgent`). Must be installed before the `agentic-platform` chart when `kagent.enabled: true`. Note: upstream does not mark these CRDs `helm.sh/resource-policy: keep`; `helm uninstall agentic-platform-crds` will remove them and cascade to all kagent CRs.
- `kagent` (`v0.9.5`, `oci://ghcr.io/kagent-dev/kagent/helm`) bundled as a conditional sub-chart (`kagent.enabled`, default `false`). All kagent resources land in `kagent.namespaceOverride` (default `kagent`) so they stay separate from the umbrella's release namespace. The `kagent-crds` chart is added to `agentic-platform-crds` as a prerequisite. Enabling kagent requires `agentic-platform-crds` to be installed first.
- `kagent.oauth2-proxy.metrics.serviceMonitor.enabled: true` — Prometheus `ServiceMonitor` for the oauth2-proxy metrics endpoint (`:44180`), labelled `observability.giantswarm.io/tenant: giantswarm`.
- `templates/kagent/netpol.yaml`: oauth2-proxy ingress CNP extended to allow scraping of the metrics port (`:44180`) from any cluster-entity source, and oauth2-proxy egress extended to include `cluster` alongside `world` on port 443 — required when the Dex hostname resolves to an internal LB VIP (private-range IP classified as `cluster` by Cilium, not `world`).
- `postgres` block: opt-in CloudNativePG `Cluster` CR (`postgres.enabled`, default `false`) provisioning the kagent application database in a named `kagent` schema (not `public`). Supports pgvector via `postInitTemplateSQL` (any CNPG version, bundled image) or the ImageVolume approach (`postgres.vector.extensionImage.reference`, CNPG 1.29+/PG18). The CNPG operator and its CRDs remain a cluster-level prerequisite. An optional Klaus sessions database (`postgres.sessionsDatabase.enabled`, default `false`) is templated but left off pending the core-runtime persistence decision.
- `templates/namespace.yaml`: renders the `kagent` `Namespace` when `kagent.namespaceOverride` differs from the release namespace, so fresh installs do not require manual namespace pre-creation.
- `templates/kagent/controller-route.yaml`: opt-in `AgentgatewayBackend` + `HTTPRoute` (`kagent.controllerRoute.enabled`) exposing the kagent controller API through agentgateway with JWT validation.
- `templates/kagent/netpol.yaml`: cross-namespace network policies for kagent (cilium and kubernetes flavors, gated on `networkPolicy.flavor`). Cilium: egress from agentgateway data-plane to kagent controller (port 8083) + ingress policy in the kagent namespace. Kubernetes: `NetworkPolicy` restricting kagent controller ingress to the release and kagent namespaces, preventing direct access that would bypass agentgateway JWT validation.
- `templates/kagent/ui-httproute.yaml`: opt-in HTTPRoute (`kagent.uiRoute.enabled`) exposing the kagent UI on the public Gateway. When `oauth2-proxy.enabled: true` routes through oauth2-proxy (port 4180); otherwise routes directly to the UI (dev only). Placed in the kagent namespace to avoid cross-namespace backend refs.
- `ci/test-postgres-values.yaml`: CI values file exercising the kagent+postgres path through `helm template`/lint.
- `ci/test-kagent-routing-values.yaml`: CI values file exercising controllerRoute + uiRoute + oauth2-proxy.
- Kagent defaults hardened for GS clusters: restricted-PSS `securityContext` applied at umbrella level (Kyverno requirement); bundled agents/tools disabled with comments explaining why and under what conditions to re-enable; Anthropic set as the default model provider (`claude-sonnet-4-6`); OTel traces and logs routed to `otlp-gateway.kube-system.svc:4317`; `controller.auth.mode: trusted-proxy` (agentgateway validates the JWT upstream and the netpols fence the controller); `oauth2-proxy` values pre-wired for Dex OIDC integration (`enabled: false` until Dex client credentials are provided).

### Fixed

- In-cluster MCP backends that listen on a non-80/443 port (e.g. the bundled `pro` / `runbooks` MCP servers on `8080`, reached via a ClusterIP) were unreachable from muster — its sub-chart CNP permits cluster-entity egress only on 80/443, so those connections timed out and the corresponding `MCPServer`s went `Failed`. The umbrella now renders a supplementary `CiliumNetworkPolicy` (`<muster>-mcp-egress`) that widens muster's egress to `networkPolicy.musterInClusterMcpPorts` (default `[8080]`; cilium flavor only; Cilium policies are additive, so no sub-chart fork). Set to `[]` to disable.
- **Muster Service name is now pinned** via `muster.fullnameOverride: agentic-platform-muster`, read directly by the umbrella's `agentic-platform.musterFullname` helper instead of re-deriving the muster sub-chart's release-name algorithm. The route `backendRef`, the `BackendTrafficPolicy` target, and `agentic-platform-mcps.musterUrl` now reference one source of truth that stays in lockstep with the sub-chart's Service regardless of release name or any future muster naming change. A blank override fails the render loudly rather than silently pointing the route at a non-existent Service (503).

### Changed

- Bumped bundled `muster` to `0.2.6` (includes the muster#772 JWT signing-key wiring fix + `jwt_key.go` enabling edge JWT validation, and the CNP ingress-gateway egress fix from muster#788).
- Bumped bundled `agentic-platform-mcps` to `0.2.4` — corrects the `identityProviders` value schema (it was `additionalProperties: false` with no properties, forbidding every provider key and making `auth.mode: exchange` unconfigurable). Unblocks multi-cluster token-exchange consumers; `forward`-only installs are unaffected.

### Removed

- **Breaking:** `gateway.enabled`, `gateway.httpRoute.*`, and `gateway.backendTrafficPolicy.*` are removed — replaced by `ingress.mode` (topology switch), `ingress.parentRefs` / `ingress.hostnames` (shared route attachment), and `ingress.backendTrafficPolicy.*`. The retained `gateway.*` keys now hold data-plane *infrastructure* only and apply in `agentgateway-*` modes. `muster.gatewayAPI.enabled` is now `false` (the umbrella renders muster's public route) — set `ingress.parentRefs` / `ingress.hostnames` instead of `muster.gatewayAPI.httpRoute.*`.

## [0.5.0] - 2026-05-29

### Added

- `agentic-platform-mcps` bundled as a conditional sub-chart (`condition: mcps.enabled`, default `false`). It renders the platform's MCP server CRs (muster `MCPServer` and/or agentgateway `AgentgatewayBackend` + `AgentgatewayPolicy`) from one abstract `agentic-platform-mcps.mcpServers` list. The toggle lives in a separate top-level `mcps:` block because the sub-chart's strict `values.schema.json` (`additionalProperties: false`) rejects an `enabled` key. The CRs consume CRDs shipped by the companion `agentic-platform-crds` chart — the umbrella still installs **zero** CRDs. New `ci/test-mcps-values.yaml` exercises the path.

## [0.4.1] - 2026-05-28

### Fixed

- Extended restricted-PSS `securityContext` defaults to the bundled valkey **metrics exporter sidecar** (`valkey.valkey.metrics.exporter.securityContext`). 0.4.0 hardened the main valkey container and its init container, but the `redis_exporter` sidecar (`containers[1]`, port 9121) lives under a separate values key and was still rejected by Kyverno's `disallow-privilege-escalation` policy.

## [0.4.0] - 2026-05-28

### Fixed

- Restored restricted-PSS compliant `securityContext`/`podSecurityContext` defaults on the bundled valkey sub-chart, which were inadvertently dropped during the CRD-chart split (0.3.0). Without them, Kyverno's `disallow-privilege-escalation` and `restrict-seccomp-strict` policies reject the `muster-valkey` Deployment because the init container reuses the main container's `securityContext` and the upstream defaults omit `allowPrivilegeEscalation: false` and `seccompProfile: RuntimeDefault`.

## [0.3.0] - 2026-05-28

### Added

- New `agentic-platform-crds` chart (`helm/agentic-platform-crds/`): a CRD-only umbrella that vendors the upstream `agentgateway-crds` (`v1.2.1`) and `muster-crds` sub-charts, shipping all five CRDs the platform's CRs consume. Install it **before** `agentic-platform`; CRD and workload lifecycles are now decoupled (two releases in sequence, Flux/Argo-agnostic). The muster CRDs are `helm.sh/resource-policy: keep`-protected via `muster-crds.crds.annotations`.
- Second CircleCI `architect/push-to-app-catalog` job (`package-and-push-crds-chart`) building and publishing `agentic-platform-crds` alongside `agentic-platform` on git tags.

### Changed

- All CRDs moved out of `agentic-platform` into the new `agentic-platform-crds` chart. `agentic-platform` now installs **zero** CRDs — it renders only the CRs (`Gateway`, `AgentgatewayParameters`, plus muster's). `helm template agentic-platform` emitting any `CustomResourceDefinition` is a regression.
- `muster.crds.install` defaulted to `false` in `agentic-platform` values, so the bundled `muster` sub-chart renders no CRDs (the currently pinned muster `0.1.197` still defaults `crds.install: true`).
- `Chart.yaml` description: CRDs now ship in the companion `agentic-platform-crds` chart.

### Removed

- `agentgateway-crds` sub-chart dependency (`condition: agentgateway-crds.enabled`) and the `agentgateway-crds:` values block from `agentic-platform`. The CRDs are provided by `agentic-platform-crds`.

## [0.2.0] - 2026-05-27

### Added

- `agentgateway-crds` bundled as a conditional sub-chart (`condition: agentgateway-crds.enabled`, default `true`) so the umbrella is self-contained. The chart now ships the `AgentgatewayParameters` / `AgentgatewayPolicy` / `AgentgatewayBackend` CRDs alongside the `AgentgatewayParameters` CR it renders; Helm applies CRDs ahead of the CR via install ordering. Disable on clusters that manage these CRDs out of band.
- Agentgateway data-plane OTel env defaults: `OTEL_EXPORTER_OTLP_ENDPOINT=http://otlp-gateway.kube-system.svc:4317` and `OTEL_EXPORTER_OTLP_PROTOCOL=grpc` added to `gateway.parameters.dataPlaneEnv`. Override to point at a different backend.
- `muster.serviceMonitor.enabled: true` default. OTel push to otlp-gateway is not yet supported by muster; ServiceMonitor is the primary observability path until muster gains an `otlpEndpoint` knob.
- `ci/test-full-stack-values.yaml` CI values file satisfying all fail-guards for a complete `helm template` render (OAuth off, valkey off, parentRefs stubbed).

### Changed

- `agentgateway-crds` is no longer a cluster prerequisite (now bundled). Muster's CRDs continue to ship via the muster sub-chart. **Caveat:** the upstream `agentgateway-crds` CRDs are not yet annotated `helm.sh/resource-policy: keep`, so uninstalling the release deletes them and cascades to all agentgateway CRs cluster-wide. An upstream change to parameterize the CRD annotations is pending; the `keep` annotation will be added here once it lands.

## [0.1.0] - 2026-05-26

### Added

- Initial agentic-platform chart bundling `muster` 0.1.197 and `agentgateway` v1.2.1.
- `Gateway` (name `agentgateway`) and `AgentgatewayParameters` overlay injecting restricted-PSS `securityContext` on the controller-rendered data-plane pod.
- `gateway.parameters.serviceType` (default `ClusterIP`) overlays `AgentgatewayParameters.spec.service.type` so the data-plane Service stays internal; the controller hardcodes `LoadBalancer`.
- `gateway.parameters.dataPlaneEnv`, `dataPlaneVolumes`, `dataPlaneVolumeMounts` strategic-merge lists on the AgentgatewayParameters overlay.
- `CiliumNetworkPolicy` for the agentgateway **controller pod** in addition to the data-plane pod (upstream agentgateway chart ships no policies).
- `networking.k8s.io/v1 NetworkPolicy` rendering when `networkPolicy.flavor: kubernetes` — best-effort (no entity selectors, no FQDN egress).
- `networkPolicy.kubernetes.{apiServerCIDR,worldExcludedCIDRs}` for the `kubernetes` flavor.
- Top-level `extraObjects: []` rendering arbitrary manifests through `tpl` alongside the chart.
- `values.schema.json` covering top-level keys with a cross-field combo check (muster valkey storage requires `valkey.enabled` or an explicit URL).
- `UPGRADE.md` documenting the breaking changes for the first stable release.

### Changed

- CRD lifecycle: `agentgateway-crds` is a cluster prerequisite (upstream agentgateway ships controller + CRDs as separate charts). Muster's CRDs continue to ship inside the umbrella via the muster sub-chart's `templates/crds.yaml`.
- Data-plane policy selector switched to the Gateway-API standard label `gateway.networking.k8s.io/gateway-name=<gateway.name>` (was `app.kubernetes.io/name=agentgateway`, which matched both the controller and the data plane).
- Controller policy selector uses the agentgateway sub-chart's selector triple (`agentgateway: agentgateway` + `app.kubernetes.io/name=agentgateway` + `app.kubernetes.io/instance=<release>`).
- Data-plane CNP gains xDS egress to the controller on TCP 9978; controller CNP gains xDS ingress from data-plane pods.
- `CiliumNetworkPolicy` egress covers `kube-dns`, `coredns`, `k8s-dns-node-cache` on 53 + 1053 (UDP + TCP); world 80/443; cluster 80/443 for in-cluster ingress (Dex / MCPServers); muster on 8090.
- Muster sub-chart's NetworkPolicy values migrate from `ciliumNetworkPolicy.*` to `networkPolicy.{enabled,flavor,cilium.allowClusterIngress,kubernetes.*}` (muster 0.1.197). Umbrella overrides `enabled: true`, `flavor: cilium`, `cilium.allowClusterIngress: true`.
- `valkey.enabled` and `muster.muster.oauth.server.enabled` default to `true`. Operators must provide `oauth.server.baseUrl`, `oauth.server.dex.{issuerUrl,clientId}`, `oauth.server.existingSecret`, and `valkey.valkey.auth.usersExistingSecret` — muster's template-time fail-guards reject install otherwise.
- `muster.gatewayAPI.httpRoute.parentRefs` / `.hostnames` no longer default to the data-plane Gateway. Muster's HTTPRoute must attach to the cluster's public Gateway (typically `envoy-gateway-system/giantswarm-default`); the muster fail-guard enforces this.
- Bundled `giantswarm/valkey-app` 0.1.2 as a conditional sub-chart (`condition: valkey.enabled`). Single Deployment + PVC; Service at `muster-valkey.<namespace>.svc:6379`. ACL-based auth: a `default` user with full permissions reads its password from `valkey-password` in `valkey.valkey.auth.usersExistingSecret`.
- Muster wired to the bundled valkey by default: `muster.muster.oauth.server.storage.type=valkey` and `storage.valkey.url=muster-valkey:6379`. Inert while `oauth.server.enabled: false`; kicks in the moment OAuth is enabled.
- `networkPolicy.flavor` enum changed from `cilium | none` to `cilium | kubernetes`. Opt out via `networkPolicy.enabled: false`.
- README is now Flux HelmRelease-first (no Giant Swarm App platform). Adds a "Gateway API CR ownership" section clarifying that only `AgentgatewayParameters` is vendor-specific to agentgateway.
- Templates grouped under `templates/agentgateway/` (Gateway, AgentgatewayParameters, four NetworkPolicy variants).
- `Chart.yaml` description reflects the runtime contract (muster + agentgateway + opt-in Valkey; CRDs as cluster prerequisite). `appVersion` stays at the umbrella's own `0.1.0`.

### Removed

- `bootstrap.oauth.*` values and the `templates/oauth-bootstrap-secret.yaml` Helm `lookup`-based Secret generator. Use `extraObjects` to ship the Secret in the same release, or pre-create it out of band and reference via `muster.muster.oauth.server.existingSecret`.

[Unreleased]: https://github.com/giantswarm/agentic-platform/compare/v1.1.33...HEAD
[1.1.33]: https://github.com/giantswarm/agentic-platform/compare/v1.1.32...v1.1.33
[1.1.32]: https://github.com/giantswarm/agentic-platform/compare/v1.1.31...v1.1.32
[1.1.31]: https://github.com/giantswarm/agentic-platform/compare/v1.1.30...v1.1.31
[1.1.30]: https://github.com/giantswarm/agentic-platform/compare/v1.1.30...v1.1.30
[1.1.30]: https://github.com/giantswarm/agentic-platform/compare/v1.1.29...v1.1.30
[1.1.29]: https://github.com/giantswarm/agentic-platform/compare/v1.1.28...v1.1.29
[1.1.28]: https://github.com/giantswarm/agentic-platform/compare/v1.1.27...v1.1.28
[1.1.27]: https://github.com/giantswarm/agentic-platform/compare/v1.1.26...v1.1.27
[1.1.26]: https://github.com/giantswarm/agentic-platform/compare/v1.1.25...v1.1.26
[1.1.25]: https://github.com/giantswarm/agentic-platform/compare/v1.1.24...v1.1.25
[1.1.24]: https://github.com/giantswarm/agentic-platform/compare/v1.1.23...v1.1.24
[1.1.23]: https://github.com/giantswarm/agentic-platform/compare/v1.1.22...v1.1.23
[1.1.22]: https://github.com/giantswarm/agentic-platform/compare/v1.1.21...v1.1.22
[1.1.21]: https://github.com/giantswarm/agentic-platform/compare/v0.5.0...v1.1.21
[0.5.0]: https://github.com/giantswarm/agentic-platform/compare/v0.4.1...v0.5.0
[0.4.1]: https://github.com/giantswarm/agentic-platform/compare/v0.4.0...v0.4.1
[0.4.0]: https://github.com/giantswarm/agentic-platform/compare/v0.3.0...v0.4.0
[0.3.0]: https://github.com/giantswarm/agentic-platform/compare/v0.2.0...v0.3.0
[0.2.0]: https://github.com/giantswarm/agentic-platform/compare/v0.1.0...v0.2.0
[0.1.0]: https://github.com/giantswarm/agentic-platform/releases/tag/v0.1.0
